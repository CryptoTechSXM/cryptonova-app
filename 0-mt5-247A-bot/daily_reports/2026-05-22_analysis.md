# 247 A/B Daily Analysis — 2026-05-22 (Friday)

> **Headline:** Incomparable day — Bot A executed 16 trades but recorded 0 closures (all missed during restart gaps, data gap critical); Bot B executed 3/22 signals via EMA filter and closed -$6.51 (1W 2L); no kill-switch on either bot; 4 restarts for A, 3 for B.

---

## Section 1 — Today's Trade Summary

### Bot A (TRAIL=$2.00 | SCALP_LOCK trigger=$1.20 / lock=$0.60)

| Metric | Value |
|---|---|
| Closed trades today | **0** |
| Wins / Losses / BEs | n/a |
| Net P&L today | **UNKNOWN** (see critical note below) |
| Win rate | n/a |
| Trades accepted (events.log) | **16** (trades_today counter reached 16 at 16:17 UTC) |
| MFE > 0 but profit < 0 | Not computable (mfe_pips=0.0 for all rows) |

**⚠️ CRITICAL DATA GAP — Bot A:** `trades.csv` has not recorded a single closed trade since **2026-05-20** (two days stale), despite 16 confirmed trade acceptances today per `events.log`. No `🏁 CLOSED` lines appear anywhere in today's `events.log`. Root cause: multiple restarts throughout the day (05:54, 07:14, 16:38, 16:39 UTC) created windows where positions were closed by MT5 trailing stops while the bot was offline. The bot never saw the close events, so `trades.csv` was never written. **This is a silent data loss bug** — the trades happened on the MT5 account (login=700090146) but are invisible to the analysis pipeline. Verify actual account P&L directly in MT5.

---

### Bot B (TRAIL=$3.00 | SCALP_LOCK trigger=$2.40 / lock=$0.80)

| Metric | Value |
|---|---|
| Closed trades today | **3** |
| Wins / Losses / BEs | 1W / 2L / 0BE |
| Net P&L today | **-$6.51** |
| Win rate (excl. BE) | **33.3%** |
| Avg profit on wins | +$4.80 |
| Avg loss on losses | -$5.66 |
| MFE > 0 but profit < 0 | Not computable (mfe_pips=0.0 for all rows) |

Bot B closed trades today:

| Time (UTC) | Side | Entry (approx) | Outcome | P&L | Source |
|---|---|---|---|---|---|
| 06:10:56 open → 09:19:10 close | SELL | 4522.95 | Win (price fell) | +$4.80 | RECOVERED |
| 11:40:23 open → 14:44:33 close | SELL | 4518.99 | Loss (price rose) | -$4.67 | RECOVERED |
| 13:12:28 open → 16:18:34 close | BUY | 4533.46 | Loss (price fell) | -$6.64 | RECOVERED |

---

## Section 2 — Signal-to-Execution Comparison

Both bots received the identical 22 ENTRY_SIGNAL messages from 4 active channels today.

| Metric | Bot A | Bot B |
|---|---|---|
| Entry signals received | 22 | 22 |
| Trades executed | 16 | 3 |
| Execution rate | **72.7%** | **13.6%** |

**Why Bot A executed 16/22:** Bot A has no EMA trend filter. It blocks primarily on news calendar (3 GBP Retail Sales blackouts at ~09:51, 09:55, 10:23 UTC) and one cooldown skip. Most signals were taken regardless of EMA alignment.

**Why Bot B executed only 3/22:** Bot B has an active EMA counter-trend filter — a **major undocumented config difference** not specified in the A/B test parameters. Of the 22 signals:
- 13 blocked by EMA counter-trend filter (BUY signals while price < EMA, or SELL while price > EMA)
- 1 blocked by time filter (01:00 UTC, outside 04:00–19:00 trading window)
- 2 blocked by MT5 calendar blackout (GBP Retail Sales m/m)
- 1 blocked by 10:00 UTC hour filter
- 3 passed all filters and were executed

> **Note for A/B integrity:** The EMA filter on Bot B is a confounding variable. The A/B experiment was designed to test trail distance and scalp-lock thresholds, but Bot B is also filtering signals differently. The execution pool for comparison is not equivalent — Bot B is operating on a curated subset of signals while Bot A takes nearly everything. Results cannot be attributed solely to trail settings.

---

## Section 3 — Channel Performance Breakdown

### Bot A (today)
No closed trades recorded — channel breakdown not possible from `trades.csv`. Per `events.log`, executed trades came from: Free Tag Signals (×2), Limitless Abundance 2.0 (×4), Limitless Abundance VIP (×3), CryptoNite MACD Premium (×5), and others. None are closeable for performance scoring.

### Bot B (today — 3 closed trades)

| Channel | Trades | W/L | Net P&L | Notes |
|---|---|---|---|---|
| RECOVERED | 3 | 1W / 2L | -$6.51 | 100% of today's closed trades |
| UNKNOWN | 0 | — | — | — |

**⚠️ 100% RECOVERED attribution today for Bot B.** Every single closed trade shows `channel_id=RECOVERED`, meaning the source channel was unknown at the time of recording (positions opened before a restart are recovered without channel attribution). This makes channel-level performance analysis completely blind for today. The channel attribution tracking needs a fix — see Section 7.

All-time from `trades.csv` (Bot B, 4 trades across 2 days): 100% RECOVERED across all entries.

---

## Section 4 — A vs B Comparison

### Today (not directly comparable — see critical note above)

| Metric | Bot A | Bot B |
|---|---|---|
| Closed trades | 0 | 3 |
| Net P&L | UNKNOWN | -$6.51 |
| Win rate | n/a | 33.3% |
| Avg profit/trade | n/a | -$2.17 |

### All-Time (from trades.csv — basis for comparison since today < 3 each)

Bot A all-time data is from **2026-05-20 only** (7 trades, all SELL, all channel_id=UNKNOWN):

| Metric | Bot A (all-time) | Bot B (all-time) |
|---|---|---|
| Total closed trades | 7 | 4 |
| Wins / Losses | 4W / 3L | 1W / 3L |
| Net P&L | **-$14.88** | **-$11.61** |
| Win rate | **57.1%** | **25.0%** |
| Avg profit on wins | +$4.20 | +$4.80 |
| Avg loss on losses | **-$10.56** | **-$5.47** |

**All-time interpretation:** Bot B's losses are substantially smaller in magnitude (-$5.47 avg vs -$10.56 for A), which is consistent with the wider trail ($3 vs $2) giving positions more room to breathe and not getting stopped out as harshly — **or** with the EMA filter simply rejecting the worst signals. Bot A's higher win rate (57.1%) is notable but the large losses when wrong drag the net P&L below Bot B's.

### Same-signal A/B outcomes today
Both bots' `trades.csv` has no overlapping closed-trade timestamps for 2026-05-22, so no direct head-to-head signal comparison is possible today. The key divergence points were the signals at 01:45, 03:59, 04:09, 05:32 UTC — all taken by Bot A, all blocked by Bot B's EMA filter.

**Trail impact observation:** Bot A's scalp lock triggered at 0.42R–0.52R thresholds on multiple positions early morning (01:45, 04:28, 05:36 UTC), engaging the $2 trail quickly. Without knowing outcomes, we can't assess whether the tighter trail exited too soon or protected well.

---

## Section 5 — Kill-Switch and Risk Status

| Event | Bot A | Bot B |
|---|---|---|
| KILL_SWITCH triggered | ✅ None | ✅ None |
| MAX_DAILY_LOSS hit | ✅ No | ✅ No |
| DAILY_TARGET_HIT | ✅ No | ✅ No |
| DRAWDOWN_ALERT fired | ✅ No | ✅ No |
| MAX_CONSEC_LOSSES | Not visible in log | Not visible in log |

**Risk limits (both bots):** MAX_DAILY_LOSS=75% | MAX_CONSEC_LOSSES=20 | DAILY_PROFIT_TARGET=3%

Neither bot is anywhere near risk thresholds today from available evidence. Bot B's net of -$6.51 from an opening CNMS balance of ~$101 represents ~6.4% drawdown — well inside the 75% kill-switch threshold.

Account balance for 247A (login=700090146) and 247B (login=700090149) cannot be confirmed from events.log (the balance figures visible in events come from the CNMS bot's own channel messages, not from 247A/B accounts). Verify balances directly in MT5.

---

## Section 6 — Bot Health Check

### Bot A
| Item | Status |
|---|---|
| Currently running | ✅ Yes — last event 16:52 UTC, latched after 16:39 restart |
| Restarts today | **4** (05:54, 07:14, 16:38, 16:39 UTC) |
| MT5 account | login=700090146 / PUPrime-Demo |
| MT5 IPC error | ⚠️ Yes — `[MT5 DOWN]` IPC timeout at **08:32:51 UTC** (same window as Bot B) |
| Errors / Exceptions | 1 MT5 IPC timeout, self-recovered |
| "Listening for signals" | Confirmed after 16:39 restart |

**4 restarts is high for one day.** Two coordinated pairs (05:54+07:14, and 16:38+16:39) suggest the restarts were externally triggered (likely by the operator, or by the Windows host). The 16:38→16:39 double-restart (66 seconds apart) looks like an interrupted restart sequence — worth checking if the env or run script had an issue at that moment.

### Bot B
| Item | Status |
|---|---|
| Currently running | ✅ Yes — last event 16:52 UTC, latched after 16:40 restart |
| Restarts today | **3** (05:53, 07:16, 16:40 UTC) |
| MT5 account | login=700090149 / PUPrime-Demo |
| MT5 IPC error | ⚠️ Yes — `[MT5 DOWN]` IPC timeout at **08:32:45 UTC** + `Manager exception` at 08:33:29 UTC |
| Recovery | Automatic — `[MT5] Connection recovered` at 08:33:31 UTC |
| "Listening for signals" | Confirmed after 16:40 restart |

Both bots hit the MT5 IPC timeout at the same time (~08:32 UTC), confirming this was a platform-side event (MT5 terminal freeze or host resource spike), not a bot bug. Bot B self-recovered in ~46 seconds.

---

## Section 7 — Recommendation Flags

**🔴 CRITICAL — Bot A trade data loss (fix first)**
Bot A's `trades.csv` is 2 days stale. All 16 trades executed today are unrecorded because their closures happened while the bot was restarting. This is a silent bug that makes Bot A's performance invisible to this analysis pipeline. Consider implementing a **startup reconciliation** step: on restart, read MT5 trade history for today and back-fill any closed trades that aren't already in `trades.csv`.

**🔴 CRITICAL — 100% RECOVERED channel attribution (both bots)**
Every closed trade in Bot B (and historically in both bots) shows `channel_id=RECOVERED`. Source channel data is being lost on every restart. Channel-level performance analysis — which signals to keep or drop — is currently impossible. The position template store (ticket→channel mapping) needs to be persisted to disk so it survives restarts. This is the most actionable data quality fix.

**🟡 IMPORTANT — Undocumented EMA filter in Bot B**
Bot B has an active EMA counter-trend filter that Bot A lacks. This is not part of the documented A/B test parameters (which only specify trail distance and scalp-lock). The EMA filter reduced Bot B's execution rate to 13.6% vs 72.7% for A. Any performance comparison between A and B is confounded by this filter — you are comparing different signal pools, not just different exit strategies. **Decision needed:** Is the EMA filter intentional on Bot B? If so, update the A/B spec. If not, disable it to make the comparison clean.

**🟡 Bot A restart frequency (4 restarts today)**
Four restarts is unusual and contributes directly to the data loss. Investigate whether the bot is crashing or being manually restarted. The 16:38→16:39 double-restart (66s gap) is particularly suspicious.

**🟡 MT5 IPC timeout at 08:32 UTC (both bots)**
Both bots lost MT5 connectivity simultaneously. Bot B recovered cleanly (~46s); Bot A appears to have triggered a restart rather than self-recovering. If Bot A's MT5 client doesn't auto-reconnect, consider adding the same recovery logic that Bot B has.

**ℹ️ Trail/scalp comparison — pending real data**
With Bot A's data gap, no definitive verdict on $2 vs $3 trail is possible today. All-time data favors Bot B for smaller losses but worse win rate. A clean comparison requires: same signal pool (or EMA filter parity), no restart gaps, and at least 3–5 trading days of complete data.

**ℹ️ Channel XFUSION SIGNALS — watch list**
Only 1 entry signal from XFUSION SIGNALS today. Insufficient data to flag, but track if it generates losing trades disproportionately.

---

## What to Watch Tomorrow

1. **Bot A data continuity:** Check that `trades.csv` gets written after any overnight closures. If it's still stale by 09:00 UTC tomorrow, the startup reconciliation fix is urgent.
2. **Bot B ahead on realized P&L:** Bot B is the only bot with readable closed-trade data. All-time Bot B at -$11.61 vs Bot A's -$14.88. Until Bot A's data gap is fixed, Bot B's record is more trustworthy for day-to-day tracking.
3. **Channel attribution:** If `RECOVERED` still accounts for >50% of tomorrow's closed trades on either bot, escalate the position-template persistence fix — the analysis is flying blind on signal quality.
4. **MT5 IPC stability:** Monitor 08:00–09:00 UTC window — both bots hit the timeout there today. If it recurs tomorrow, the MT5 terminal or host needs attention.
5. **Kill-switch proximity:** Neither bot is near limits today. Continue monitoring; a bad session with Bot A's wide execution could accelerate drawdown quickly.

---

*Report generated automatically by scheduled task — 2026-05-22*
*Sources: trades.csv, signal_intake.csv, events.log for 0-mt5-247A-bot and 0-mt5-247B-bot*
