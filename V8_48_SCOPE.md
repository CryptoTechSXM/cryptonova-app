# V8.48 — LOCKED SCOPE (owner decision 2026-08-09: NOTHING DEFERS)

Policy, owner's words: *"I want everything we find placed in the scope for v8.48
and not defer to later versions, we keep pushing back deploy bcuz we defer, cost
is building."* Every finding below ships in V8.48. This file is the execution
list; `V8_48_BACKLOG.md` holds the evidence for each.

## ⛔ DEPLOY GATE — VERIFIED AGAINST SOURCE 2026-08-11

Owner rule: **do not deploy while anything deploy-requiring is unfinished.** Every row
below was grepped against `contracts/` rather than trusted from this file — two items
were already implemented and still marked open, and item 11 turned out to be LIVE money
loss that item 12 would have accelerated.

### STILL OPEN — each one blocks the deploy

| item | what | file |
|---|---|---|
| ~~2~~ | ✅ **DONE 2026-08-13 (534 passing — see the count note in the item-2 row below).** `reservedHeldFor(member)` on TierRouter + per-matrix `reservedHeldOf`; behavior unchanged, one shared internal with claimableOf so view and enforcement cannot diverge. | TierRouter(Lib) |
| 7 | No cross-pair `memberJoinedAt`, so `earlyExitPenaltyBps` cannot work. Zero occurrences. | PairManagerV8 |
| ~~8~~ | ✅ **DONE 2026-08-11.** Clamped to `balanceOf`. Measured note: removing the clamp does NOT fail the tests, because item 9 keeps batches within the balance and every burn reaches `_update` — it is a guard against a FUTURE path that moves balances without touching batches, which is exactly how this bug arrived. | CNOVAToken |
| ~~9~~ | ✅ **DONE 2026-08-11 (508 passing).** Burns bypass the vest guard by design (`to == address(0)`), so batches outlived the tokens and `available` pinned to ZERO — the wallet could not move ANY tokens, including unlocked ones acquired later, until the stale batches matured. `_reduceVestAfterBurn` now brings the ledger down, reducing the LATEST-unlocking batches first so the holder keeps those closest to maturing, and pops emptied slots so the 200-batch cap is not leaked. Four mutations killed, including one proving the `lockedSum <= bal` guard is load-bearing (without it, burning ordinary UNLOCKED tokens underflows and reverts). Also corrected `burnFrom`'s doc, which claimed "vesting lock still applies" — it never did. | CNOVAToken |
| ~~40~~ | ✅ **DONE 2026-08-13 (556 passing) — contract half + the claim verified.** See the item-40 row in the contract-changes table. | FigureEightMatrixV8 |

### THE PARKED LOOP — MEASURED 2026-08-12, BLOCKS THE DEPLOY (owner-raised)

`scripts/diag_parked_growth.js`, full-window scan (V8.47 deploy → block 45,397,200,
~7.8 days, zero failed ranges). Three findings, one per section of the output:

1. **TRAJECTORY.** 23,069 parks; queue net-growth ~+125/day (991 live: T2 299,
   T3 290, T1 175 — the HIGHER tiers dominate, so shortfalls and loans are big).
   Park rate accelerating: 1,849/day (first half) → 3,302/day (last 3 days).
   Events-vs-live bookkeeping: cumulative net +1,460 vs 991 live — ~469 queue
   exits happen via paths that emit no Self/CoPay/Evict event (exitSeat,
   keeper reclaim); noted so nobody chases it as a hole later.
2. **LOOP SIGNATURE.** 650 unique parkers out of ~671 members total. Histogram:
   1x: 8 · 2x: 14 · 3-5x: 77 · 6-10x: 28 · **11+x: 523**. Repeat share **99.8%**.
   Parking is not an edge case; it is the member lifecycle. Mechanism: earnings
   per cycle ≈ 34% of fee, crossing reserve 50% → structural ~16% shortfall at
   every cycle-out (the long-measured 84% cluster) → park → rescue → repeat.
3. **FINANCING — the actual exponential.** SF daily net-outstanding delta:
   $7 → $13 → $44 → $459 → $1,707 → **$4,632**. Outstanding **$6,952** (contract
   counters; event totals were ~$30 short — dropped ranges, counters win), 91%
   accrued in the last 48h. 08-12 alone: loaned $8,481, repaid $3,849. Every
   copay loan repays FIRST from the member's next earnings, widening their next
   shortfall — debt compounds per member. On mainnet this is the SF underwriting
   ~16% of every re-entry, forever, at an accelerating rate.

**BOTH OPEN QUESTIONS ANSWERED 2026-08-13:**
- **The 08-12 mix flip — RESOLVED, not an outage.** Fastlane healthy (~1 fast-lane/
  hour, its long-run rate); NO bot jobs exist on the VPS; the self-rescue volume was
  BIGFILL running from the OWNER'S WINDOWS MACHINE (owner statement; stopped 24h+ as
  of 08-13 — see CLAUDE.md "WHERE TRAFFIC COMES FROM"). The stop unmasked the loop.
- **Debt concentration — MEASURED (`scripts/diag_debt_rank.js`, 577 borrowers, no
  holes, event sums == SF counters to the cent):** outstanding **$7,287.95** across
  **412** current debtors. **THIN**: top 1% hold 7.4%, top 10% hold 38.5%; histogram
  mass in $1–$50; nobody over $200. NOT a few whales — the mechanism hits everyone.
  **64 wallets already owe ≥ ~1 full cycle's earnings ($1,917 = 26% of the book) —
  their next shortfall is guaranteed** (the insolvency-floor cohort). Top borrower
  0x1acc0225 ($117, T7) was copay-rescued again DURING the measurement session;
  0x09d160f2 has taken 11 loans. Lift table re-run on today's population
  (`diag_parked_truth.js`): ZERO of 245 sampled self-funded; median contribution
  79.5% of fee; **the lift table has a CLIFF — +5/+10/+15 points lift 1%/3%/8%,
  +20 points (70% reserve) lifts 49%** because the 80–90% band holds 114 of 245.

**DECISIONS (owner, via picker, 2026-08-13):**
(a) `CROSSING_RESERVE_BPS` — ✅ **RESOLVED 2026-08-13: KEEP 5_000, stays a hardcoded
constant (not made governable — no reason to spend bytes on a dead lever).**
`scripts/model_reserve_bps.js` was built (strict reads only, plumbing verified
on-chain first: splits identical across all 34 live matrices, 4750 + 5000 + 250 =
10000) and run against live Base Sepolia the same day, 296 sampled of 990 queued.
**THE RESULT KILLS THE LEVER, NOT JUST THE +20 CANDIDATE: dynamic lift is 0% at
EVERY candidate 5500–7500, against the static table's 44–49%.** The arithmetic:
under a pro-rata rescale the earnings ratio a member NEEDS to self-fund is
`(10000-R)/(9750-R) x 47.5%`, which RISES with R — 50.0% at R=5000 → 52.8% at
R=7500 — because a bigger reserve shrinks the member's earnings faster than it
shrinks the gap. Measured medians are 21.8–34% of fee (T2 21.8 / T3 23.5 / T1+T4
34.0); no reserve value reaches them, in either direction. And the cost side for
zero lift: at 70% reserve, member money per $100 falls $39.00 → $23.63 and
treasury/floor backing $5.00 → $2.89; the SF loan-to-inflow ratio actually WORSENS
(8.3x → 9.3x at 75%). This confirms the long-recorded FUNDING CONSTRAINT design
fact — 39% of a fee reaches members while self-funding needs 50%+; referral income
closes that gap, not the reserve. **Per-member law the model rests on (from the
repayment mechanics): a loop member either clears `R/10000 + e*s >= 1` (debt → 0)
or their debt grows by `fee*(1 - R/10000 - e*s)` EVERY cycle — converge or
compound, no middle band.** The parked loop is therefore attacked ONLY by items
46+47. Model output archived in the session log; re-run the script for fresh
numbers before ever reopening this.
(b)+(c) — **SCOPED AS A PACKAGE: items 46 + 47 below — ✅ BUILT 2026-08-13 together
with the item-45 fix (553 passing / 0 failing, suite `V8_48_GhostFloor.test.js`,
13 tests). THE PARKED LOOP NO LONGER BLOCKS THE DEPLOY** — remaining blockers are
items 7 and 40 only. Details in the item-45/46/47 rows.

### NEEDS AN OWNER DECISION BEFORE IT CAN BE BUILT

| item | the decision |
|---|---|
| ~~42~~ | ✅ **RESOLVED 2026-08-12** — epoch policy decided and shipped. See item 42 in the contract-changes table below. |
| ~~2~~ | ✅ **DECIDED 2026-08-12 — CORRECTED SAME DAY: option (b) — stay high-tier + `reservedHeldFor(member)` getter.** A first picker answer chose (a) "bind across all tiers"; the owner then said he had not understood the item and asked for pros/cons, and chose (b): keeping today's soft semantics (automation waits when underfunded) over a cross-matrix withdrawCore rewrite right before the deploy. Record kept so no future session "restores" decision (a) from the earlier commit message. |
| ~~28~~ | ✅ **DECIDED 2026-08-12: KEEP the 30-day expiry — CLOSED, no contract change.** The surfacing half (claim deadline date + urgency warning) shipped to members with the 2026-08-12 main promotion. Sweep-back to the pool is now the stated rule; ~$1,865 recycles 2026-09-04 unless claimed. |

### ALREADY DONE — was marked open, verified present in source

| item | evidence |
|---|---|
| 26 | `communityOverflowBps` present in `StabilityFund.sol` (4 refs) — shipped `bb081e0`. |
| 33 | `_pairFull(pairs.length - 1)` is the live spawn trigger in `_tryAdvancePair`, with its own V8.48 item 33 comment. |
| 30 | `deployEntryThreshold` survives only in two COMMENTS — no code references. Properly removed. |

## Contract changes

| # | change | contract | source |
|---|---|---|---|
| 1 | ✅ **IMPLEMENTED 2026-08-10 (456 passing).** `freeWithdrawable()` now mirrors `withdrawCore` exactly: debt off the top, automation reserve read ONLY in the member's highest tier, crossing lock applied ONLY when `automationReserve > 0`, and 0 returned where `withdrawCore` would revert. Added `netClaimableOf(member)` = claimable minus the 1.5% withdrawal fee. **A DOUBLE-DEDUCTION WAS CAUGHT BEFORE COMPILING:** `pendingPoolOf` already nets the debt clawback, so subtracting the full `memberDebtOf` on top of it would have removed the clawback twice — `_poolShareGross` was split out so the debt is applied ONCE to the gross, matching what `withdrawCore` nets out to. **Body lives in `MatrixLogicLib.claimableOf`, not the matrix:** `MatrixPairFactory` EMBEDS the matrix creation bytecode, and inlining this took it to **108 bytes** of EIP-170 headroom; moving it to the linked library left the factory at **799** — 443 BETTER than before this item, and FigureEightMatrixV8 smaller than it started. **Test: `V8_48_Withdrawable.test.js` (4) asserts the VIEW EQUALS THE PAYOUT** — reads `freeWithdrawable`/`netClaimableOf`, performs the real withdrawal, requires they equal the change in `totalWithdrawn` and in the wallet balance. That comparison never existed, which is why the bug survived releases. Preconditions are asserted, not wrapped in an `if`, after the first draft could have skipped its key assertion silently. **One existing test adjusted, assertions untouched:** `V8_44_UX.test.js` UX4 needed free earnings BELOW the fee to exercise `hybridUpgrade`'s shortfall path, which only held because the old view locked the crossing reserve for an opted-out member; referrals 6 -> 4 restores the shortfall. It failed on its own SETUP GUARD, which is why the diagnosis took seconds. ORIGINAL: `freeWithdrawable()` becomes a line-for-line mirror of `withdrawCore` (V8.32 opt-out + debt). Add `netClaimableOf(member)`. **CONFIRMED ON-CHAIN AND QUANTIFIED 2026-08-10 — the view under-reports on EVERY account measured.** Six-account cohort: `0x09D160` view $33.15 vs claimable $340.23 (under by **$307.09**); `0xa2f6FB` $18.41 vs $313.01 (**$294.60**); `0x7a245E` $85.55 vs $268.28 (**$182.73**); three smaller accounts all report **$0.00** against real balances. **Proven by withdrawal, not by reading:** `freeWithdrawable` summed to $0.00 for `0x1C56C6` while that member withdrew **$124.99** in two transactions minutes later. **ROOT CAUSE:** `freeWithdrawable` applies the crossing-reserve lock in EVERY matrix; `withdrawCore` applies it only where `automationReserve > 0`, i.e. the member's HIGHEST tier (`highest - 1 == cfg.tierIndex`). In every other matrix the real withdrawal path has no lock at all. Secondary divergence: `withdrawCore` repays SF debt off the top, `freeWithdrawable` does not model debt. **The frontend already knows and works around it** — `index.html:6626` computes the headline itself as *"raw withdrawable + pendingPoolOf"* and carries the note *"Contract backlog for next redeploy: make the freeWithdrawable VIEW mirror"*, which is why the dashboard is correct while the contract view is not. Direction is SAFE (understates, never over-promises) but a member reading it would believe their money is locked. **Also verified while testing:** withdrawal SETTLES pending pool first, so `totalEarned` understates until settlement ($74.52 -> $174.48 on one account across a single withdrawal); and the SF clawback works exactly as the loan panel promises ($15.25 borrowed, $15.25 repaid, $0.00 owed after withdrawing). | FigureEightMatrixV8 / MatrixLogicLib | §1, measured 2026-08-10 |
| 2 | ✅ **IMPLEMENTED 2026-08-13 (534 passing) — option (b), owner-decided 2026-08-12 after pros/cons: STAY high-tier-only, add the getter; behavior UNCHANGED.** `TierRouter.reservedHeldFor(member)` returns what the crossing lock + automation reserve ACTUALLY withhold right now — the on-chain version of the frontend's `_claimableAll.heldNow` (index.html:6966), so UIs stop reconstructing it from five reads per matrix. **STRUCTURE (the item-1 discipline, enforced by construction):** `MatrixLogicLib.claimableOf` was refactored into one shared internal `_claimableAndHeld` returning (claimable, held); `claimableOf` and the new `reservedHeldOf` are two views over ONE arithmetic, so the view cannot diverge from what withdrawCore enforces without breaking item 1's own suite. The DEBT portion of a balance is deliberately NOT counted as held — withdrawCore repays SF debt BEFORE the holds apply, so that money is held toward repayment, not the reserve target (RH6 pins this). Matrix gained a thin `reservedHeldOf(member)` wrapper (body in the linked library — factory embeds matrix bytecode, ~799 bytes headroom; `compile --force` printed NO size warning). `TierRouterLib.heldTierForMember` walks one tier's pairs and sums — STRICT, no try/catch: a view's failed read must revert, never return a plausible zero (the 2026-08-12 fabricated-fallback class). The router view reads ONLY the member's highest tier, the sole place withdrawCore enforces the reserve; it can legitimately read well below `reservedFor(member)` — that gap is the best-effort design, not a bug. **TESTS `V8_48_ReservedHeld.test.js` (6):** RH2 is the enforcement check — perform a REAL full withdrawal and require the balance left behind to equal the view's prior answer; RH1 partition (free + held == withdrawable + pool) and composition (held == crossing shortfall + reservedFor while free > 0) plus the router sum; RH3 tier scoping (T2 sponsor-commission money is never held while the member's highest is T1); RH4 the V8.32 opt-out reads zero; RH5 an all-held small balance: view says all-held AND the withdrawal reverts — agreement, item-1 doctrine; RH6 booking debt moves the FREE side only, and the post-withdrawal remainder still equals held. Fixture note: RH5 deliberately avoids the 14-seat seeding — Q and R would be the 15th/16th entries and the 16th rotates the root. **COUNT RECONCILIATION:** the handoff's "514 passing" headline was STALE — the true pre-item-2 baseline was 528 (this table's items 4/5/6 row), so 534 = 528 + 6 and nothing unexpected ran. **POST-DEPLOY frontend task (same pattern as item 3's):** `_claimableAll` can drop the client-side heldNow reconstruction and read `reservedHeldFor` — feature-detect across the V8.47→V8.48 cutover; the reserve badge at index.html:5955/:5963 is the consumer. | TierRouter(Lib) / MatrixLogicLib / FigureEightMatrixV8 | §2 |
| 3 | ✅ **IMPLEMENTED 2026-08-12 (520 passing).** `TierRouter.bulkWithdraw(uint256 amount)` — ONE signature draws up to `amount` GROSS across every matrix: lowest tier first, pair 0 first, MatA before MatB; each matrix takes its 1.5% fee out of what it pays; `withdrawCore` repays SF rescue debt off the FIRST draws exactly as a direct withdrawal would. Each take is capped at `freeWithdrawable()` — the item-1 mirror, which returns 0 anywhere withdrawCore would revert — so the expected path never reverts on amount, and a matrix that fails anyway is SKIPPED (soft, the `drawFreeEarnings` doctrine): **one matrix can no longer sink the whole withdrawal**, which is the defect this item retires (Deborah's failed $50 2026-08-10; CryptoJan22's "clicked max, only 50% went through" 2026-08-11 — and for EIP-7702 smart accounts every per-matrix leg was a separate MetaMask relayer round-trip, found 2026-08-12, making the loop still worse than it looked). **A PARTIAL FILL IS SUCCESS** — what could move, moved; reverts only when NOTHING was drawn (`TRState`), so a member never pays gas for a no-op reported as done; `amount == 0` is `TRZero`. Matrix wrapper re-signed pre-deploy: `routerWithdrawFor(member, amount)`, 0 = the V8.44 full sweep (V8.48 is a fresh deploy — no on-chain caller holds the old selector). **EIP-170 INCIDENT, CAUGHT BY THE COMPILER, NOT THE SUITE:** adding the overload put TierRouter at **24,724 bytes — 148 OVER the deploy limit — while all 520 tests passed**, because hardhat's test network allows oversized contracts; the suite is NOT a size gate and a green run proves nothing about deployability. Fixed by moving BOTH sweep loops (V8.44 full + new partial) into TierRouterLib (`sweepTierToMember` / `drawTierToMember`, shared `ILPair`/`ILMat` pair walk) and deleting the router's `_sweepMatrix`; `--force` compile now prints no size warning. **TESTS `V8_48_BulkPartial.test.js` (6), expected values derived from `freeWithdrawable` per matrix** (the same view-vs-transaction method as item 1): BP2 pins DRAW ORDER by requesting exactly f1 + 1 micro-unit — a reversed tier loop pulls ~f2 from T2 instead of 1 unit; BP6 first re-verifies the item-1 mirror models a freshly booked debt (`f1d == f1 - debt`) and then proves the sweep clears the debt while paying net of the REQUEST; setup preconditions asserted loudly (item-11 lesson) — W1 must hold free earnings in BOTH tiers (L1 on two registrations + the V8.45 sponsor commission on M's manualUpgrade, i.e. earnings in a tier W1 never joined, exactly the state the old loop mishandled). **FRONTEND CAVEATS FOR THE POST-DEPLOY SWITCH:** (1) the overload makes `tr.bulkWithdraw` AMBIGUOUS in ethers v6 — call by full signature `tr["bulkWithdraw(uint256)"]` / `tr["bulkWithdraw()"]`; `V8_44_UX.test.js` UX5 was updated for exactly this, and `index.html`'s TIER_ROUTER_ABI must declare both signatures explicitly when it adopts the new call. (2) Switching `doWithdrawPartial`'s per-matrix loop to the single call is a POST-DEPLOY task — the live V8.47 chain has no such function; feature-detect or gate on the deploy, same pattern as the CW schedule reads. | TierRouter(Lib) / FigureEightMatrixV8 | §3 |
| 4 | ✅ **IMPLEMENTED 2026-08-12 (528 passing) — Option A, owner-decided 2026-08-08.** `mintReward(to, tierIndex, deposit6)`: the caller passes the EXACT reserve deposit this seat just made (`_distributePayments` now returns its `treasuryAmt` — never a fee table that would drift), and the mint is capped at `deposit6 * 1e18 / floorPrice()`. Strict: a seat that deposited nothing mints NOTHING; treasuryRef unset = cap not applied (same guard as the FF branch, keeps every non-wired fixture valid). **ORDERING CORRECTION to the V8_48_BACKLOG model:** deposits run BEFORE the mint, so a bound mint is slightly floor-POSITIVE relative to the pre-entry floor — the honest invariant, proven in FG2, is "no seat leaves the floor lower than it found it" (vs the intra-tx post-deposit peak any mint dilutes, necessarily). Signature ripple: MatrixLogicLib passes the real deposit; legacy V6/V7 call sites pass 0 (they made no deposit, they mint nothing against a V8.48 token); 39 test call sites gained `, 0` mechanically (no old fixture wires treasuryRef — verified before the sweep). **A stack-too-deep in `_distributePayments` was fixed by SCOPING each split's local (item-12 doctrine: remove locals, never enable viaIR).** Tests: `V8_48_FloorGuards.test.js` FG1-FG4 — launch equation ($0.50 @ $0.01 seed = exactly the scheduled 50), binding cap with `minted == deposit/floor` exact, strict zero-deposit, no-ref passthrough. | CNOVAToken / MatrixLogicLib | §4 |
| 5 | ✅ **IMPLEMENTED 2026-08-12 (528 passing) — the hard guard, owner-decided 2026-08-07, no override, no timelock escape.** `floorBefore` snapshotted at entry, `require(floorPrice() >= floorBefore, "Treasury: floor would drop")` after the funds move, in BOTH functions. Consequences are the DESIGN and are asserted as such: FG5 proves emergencyWithdraw REVERTS against reserve once supply exists ("unusable against reserve" — the decision, verbatim); FG5b proves it still works pre-launch (supply 0 → the seed floor is constant on both reads); FG6 proves addDexLiquidity spending reserve reverts even through a compliant router (MockDexRouter + MockMemberCount open Universe Mode's 500-member gate in test). Reserve-funded DEX liquidity is now impossible until a future design routes NON-reserve funds — decided with eyes open. | CNOVATreasury | §4 |
| 6 | ✅ **IMPLEMENTED 2026-08-12 (528 passing) — and the fix EXPOSED A LATENT DILUTION BUG the item text never knew about.** `_floorPriceE6()` now returns `ITreasuryFloor(treasury).floorPrice()` (one formula, one place; the supply-0 gate stays local so pre-launch buys still revert as before). The 7 test failures that followed were the POINT: the DirectSale fixture faked the treasury with a bare signer, and the real contract delivered `toTreasury` by PLAIN TRANSFER — invisible to `usdcReserve`, so with item 6 alone every sale would have minted supply against an unmoved reserve and DILUTED the floor (the exact opposite of the documented "floor-neutral by design"; invisible while the floor read balanceOf). Completed fix: buyCNOVA deposits the floor-backing portion via `forceApprove` + `depositReserve()`; **deploy_v8.js gained `treasury.setAuthorizedCaller(directSale)` — WITHOUT it every purchase reverts "Treasury: caller not matrix"**; the fixture now deploys the real CNOVATreasury seeded through depositReserve (same $25,000 floor, all old assertions keep their numbers). FG7 proves loose USDC sent straight to the treasury no longer moves either view. | CNOVADirectSale / deploy_v8 | §4 |
| 7 | Real cross-pair `memberJoinedAt` so `earlyExitPenaltyBps` can work | PairManagerV8 | §7 |
| 8 | `lockedBalanceOf` returns `min(sum(batches), balanceOf(wallet))` | CNOVAToken | §8 |
| 9 | Prune/reduce vest batches on BURN inside `_update` | CNOVAToken | §8 |
| 42 | ✅ **DECIDED AND IMPLEMENTED 2026-08-12 (514 passing).** Epoch policy set from MEASURED emission, not from the tier-mix theory this item originally carried. `epochMemberLimit` **10,000 → 1,000**, `epochTimeLimit` **30 days → 180 days**, `epochMintLimit` **UNCHANGED at 1,000,000**. Declared DEFAULTS were changed, not a post-deploy transaction: `deploy_v8.js` never sets these three, so a fresh mainnet deploy takes them straight from the contract and there is nothing anyone can forget to run. **THE MEASUREMENT (`scripts/model_epoch_policy.js`, live V8.47 set):** 27,776 `TokensMinted` events across **671 unique members** — 41 seats and 164 tier-multiplier units per person. `mintReward` fires on EVERY seat while `countedMember` counts a person ONCE, EVER, so MINT and MEMBER are denominated in quantities ~41x apart. Straight off the chain: **epochs 2, 3 and 4 cost 2,150,000 CNOVA and were bought by 26 new members.** Epoch 4 shows `MEMBER: 0 of 10,000`. **NOTHING WAS LEAKING:** predicted treasury inflow (seats × fee × 5%) came to $69,063 against an actual `usdcReserve` of $69,051 — ratio **1.000**. Every seat paid a real entry fee; cycling is how members earn and it is fully funded. The fault was entirely in the limits. **WHY MINT CANNOT BE RAISED — this is arithmetic, not preference.** Final Frontier mints `rewardPct * TREASURY_PER_ENTRY / (100 * floorPrice)` = **$0.375 of CNOVA per $0.50 of treasury inflow**, constant in dollars and shrinking in tokens as the floor rises, so supply grows as the treasury to the power 0.75. FF's entire runway is the headroom the fixed schedule leaves: bonus era ending at **8M → treasury may grow 3.6x** before MAX_SUPPLY; ending at **20M → 6.7%**. The second is not a shorter runway, it is none. And `mintReward` **TRUNCATES at MAX_SUPPLY, it does not revert** — members silently receive zero and `MatrixLogicLib`'s `try ... {} catch {}` reads that as success. The governance menu offers 2.5M and 5M for this parameter; both end Final Frontier on arrival. **WHY 10,000 WAS DEAD:** with MINT pinned at 1,000,000 and Genesis at 50, MEMBER can only lead if members average under `1,000,000 / (10,000 * 50)` = **2 units each** — one T1 seat and barely a second, in a matrix built to cycle. It showed the community a progress bar that could not move. Crossover at 1,000 is **20 units/member**; testnet measured 164 under bot churn against matrices that fill in hours, real users estimated 10-40. `epochMemberLimit` carries **no supply risk either way** — MINT caps the epoch regardless — so all it controls is how many people share the bonus rates: first 1,000 at 50x, next 1,000 at 40x, then 20x, 10x, 5x, ~8,000 members through the fixed schedule. **WHY 180 DAYS:** at 30 days the whole nine-epoch schedule expires in eight months whether or not one person joins, spending the Genesis bonus on the calendar instead of on early members. 180 makes TIME a dormancy backstop. No supply cost — MINT is still the cap. **ALL THREE VALUES ARE ON THE V8Governance MENU** (`[100_000e18, 500_000e18, 1_000_000e18, ...]`, `[100, 500, 1000, 5000, 10000, ...]`, `[7d, 14d, 30d, 90d, 180d, 365d]`). This was nearly got wrong: **2,500 was the recommended member limit for one draft and is not on the menu** — a value set by admin but absent from the list is one the DAO can change and never vote back. **TESTS: `V8_48_EpochPolicy.test.js` (6), and deliberately NOT `expect(limit).to.equal(1000)`** — a change detector fails on any edit and explains nothing. Each test encodes the REASON: 8 epochs × mintLimit must fit in half of MAX_SUPPLY; MEMBER must lead MINT in epoch 1 at 20 units/member AND the old 10,000 must still fail that same invariant; every default must appear on its governance menu; TIME ≥ 90 days; and the failure mode is PROVEN — at MAX_SUPPLY a T10 member asks for 32,000 CNOVA, receives 100 wei, and nothing reverts. **THREE TESTS HARDCODED THE OLD 30-DAY DEFAULT** and now read `epochTimeLimit()` off the contract: `V8Elevator` 5d (which also gained the missing just-short-of-the-window assertion it never had) and `V8_46_EpochMemberCount` line 66. The last was missed by the first sweep because it used a bare `30 * 24 * 3600` and never names the parameter — **grep the literal, not only the identifier.** `V8Elevator` 6d already pinned `setEpochTimeLimit(365 days)` explicitly, which is why it survived; that is the pattern to copy. **ORIGINAL ITEM TEXT AND ITS SUPERSEDED THEORY, kept because the reasoning was wrong in an instructive way:** **EPOCH BURN RATE — MEASURED 2026-08-12, all three advances were MINT.** The token is on epoch 4 of 9 after SEVEN DAYS. `EpochAdvanced` events: epoch 2 on 08-09, epoch 3 on 08-10 (1.57d later), epoch 4 on 08-11 (1.26d later) — **every one TRIGGER_MINT**, never MEMBER. The MEMBER counter reads **0 of 10,000**. **WHY: the mint limit is denominated in CNOVA, but CNOVA-per-entry scales 640x across tiers** (`tierMultipliers = [1,2,4,8,20,40,80,160,320,640]`). Against `epochMintLimit = 1,000,000` at the Genesis base of 50: T1 needs 20,000 entries to burn an epoch, T7 needs 250, **T10 needs 31**. So the schedule is governed by TIER MIX, not adoption — and the stress jobs ran `MIN_TIER=7 MAX_TIER=9`, exactly the traffic that burns it fastest. Base reward is now 10 CNOVA, 20% of Genesis; a T10 entry that would have minted 32,000 now mints 6,400. **`totalMinted` is 3,150,370 of 21,000,000 — 15% of max supply in a week.** **THE DECISION IS ECONOMIC, NOT TECHNICAL.** The epoch names (Nebula Genesis, Mercury Rise, Lunar Cluster) read as ADOPTION milestones, but nothing ties them to member growth; MEMBER is the only trigger that does and it is unreachable except on T1/T2 traffic. Options, none of which Claude should pick: (a) raise `epochMintLimit` so mint stops binding first; (b) lower `epochMemberLimit` so MEMBER becomes the real gate and epochs track adoption as the naming implies; (c) reduce the high-tier multipliers so one whale entry cannot consume 3% of an epoch; (d) accept it and say plainly that epochs are an emission schedule, not an adoption one. All three limits are governable (`setEpochMintLimit` / `setEpochMemberLimit` / `setEpochTimeLimit`) and multipliers via `setBoostTable`-style governance. **EPOCHS DO NOT GO BACKWARDS — this is a pre-mainnet decision only.** Evidence: `scripts/diag_epoch.js`. **That theory — that TIER MIX drives the schedule — was measured and is only half right.** Tier mix sets units per SEAT; what actually burns the epochs is SEATS PER MEMBER (41), which no option in the original list addressed. Options (a) and (c) are now ruled out by the Final Frontier arithmetic above; the shipped policy is (b), with TIME corrected as well. | CNOVAToken | §42, measured 2026-08-12 |
| 10 | ✅ **IMPLEMENTED 2026-08-09.** `dest = p.matrixA`, unconditional. NO collision branch: V8.46's UNIVERSAL PAIR GUARD (MatrixLogicLib:278) already rejects a seat in EITHER half, so steering an already-seated member to MatB swaps one revert for another — my first draft did exactly that and the test caught it. MatrixKeeper:558 treats the revert as expected-and-swallowable. Tests: `test/V8_48_RescueRouting.test.js` (3). ORIGINAL: `rescueReentry` returns own MatA — mirror `TierRouterLib.sameTierTarget` incl. the V8.46-C collision guard | PairManagerV8 | §10 |
| 10b | ✅ **IMPLEMENTED 2026-08-09 — final rule: share the stream across every FULL pair, plus exactly ONE pair being filled.** No configured threshold at all; capacity is read from the matrices (`occupancy()` vs `MATRIX_SIZE()`), turn-taking from `totalRegistrations`. **Three wrong attempts first, each caught by a test — recorded so they are not retried:** (a) *round-robin over all pairs* — spreads entries so no pair reaches MATRIX_SIZE, and below that NOTHING rotates (MatrixLogicLib:407); T1.3 at 8 members / rot=0 is what that looks like. (b) *strict one-door* — pairs 1+ never receive externals, never rotate; broke the O4 design-law gate. (c) *divert at physical fullness* — a full MatA only rotates when it RECEIVES an entry, so diverting away from a full pair FREEZES it; broke "pair-0 MatB must rotate WITHOUT any keeper". The two needs genuinely conflict — a full pair needs a CONTINUING stream, a filling pair needs a CONCENTRATED one — and the final rule serves both. `routeEntryThreshold` is now DEAD (only `overflowActive`, itself dead code, still reads it). ORIGINAL: **`_findExternalPair` cumulative-counter exclusion — THIRD site of the V8.46 root cause, never fixed.** `pairs[i].totalRegistered < routeEntryThreshold` with a counter that only ever increments (:426). A pair that passes the threshold is excluded from new registrations FOREVER — its MatA stops receiving entries and freezes. Masked since 2026-07-27 by `route_rr.js`. Fix: `routingIdx = totalRegistrations % pairs.length` — round-robin. Equal share regardless of history, no monotonic trap, O(1) (deletes the loop), and exactly what `route_rr.js` imposes externally. Feeding a full MatA is safe: V8.41 root rotation frees the seat (regression S3). **NOT argmin** — cumulative totals across pairs born at different times are not comparable; live `[3483,595,8]` would send 587 consecutive entries to T1.3 and starve the other two. | PairManagerV8 :578-584 | NEW, verified 2026-08-09 — **live incident, see below** |
| 11 | ✅ **IMPLEMENTED 2026-08-11 (501 passing).** `surplus = effectiveContrib > entryFee ? effectiveContrib - entryFee : 0` and `withdrawable = surplus` instead of `0`, in BOTH `selfRescue` and `coPayRescue`. The USDC backing it never left — `_finalizeCrossing` forwards only `entryFee` — so the credit is fully backed, asserted in the test by checking the matrix balance moves by exactly the fee. **THIS WAS LIVE MONEY LOSS, NOT A LATENT ONE:** `fastlane.log` 2026-08-11 shows $5.00 + $5.438759 against a $10.00 fee and $12.50 + $14.76495 against $25.00 — **$0.44 and $2.26 erased**, roughly one an hour. **AND ITEM 12 WOULD HAVE AMPLIFIED IT:** item 12 makes self-funded rescues fire in 5 minutes instead of 24 hours, and self-funded is precisely the condition under which the surplus exists. Shipping 12 without 11 would have increased the rate of the loss. Found by asking, before deploying, whether anything deploy-requiring was still open. **THE FIRST TEST WAS WORTHLESS AND A MUTANT SAID SO:** the fixture cannot produce a self-funded parked member by accident — they cluster JUST under the fee (best $9.875 of $10.00 locally, the same 91-98% band as the live chain) because withdrawable freezes at park time, so the test skipped its own assertion and erasing the surplus again passed all 3. Rebuilt to CONSTRUCT the state: park a member, then register under them, since an L1 credit reaches a referrer whatever their own state — which is how the live self-funded members got there too. Preconditions are now asserted loudly, never skipped. Three mutations all killed, including a deliberate over-credit. ORIGINAL: **selfRescue / coPayRescue surplus loss** — `crossingReserve` and `withdrawable` are zeroed UNCONDITIONALLY while `_finalizeCrossing` forwards only `entryFee`. Surplus is erased. Must credit the excess back to `withdrawable`. | MatrixLogicLib :1265-66 | NEW, verified 2026-08-09 |
| 12 | ❌ **CLOSED NOT-A-DEFECT 2026-08-11 — the premise was false and the fix was measured, built, and thrown away.** ORIGINAL: *"`checkUpkeep` must DISCOVER WORK_PARKED_RESCUE, WORK_EVICT_PARKED (and GHOST/RECLAIM). Executable today but undiscoverable — retires 3 keepers."* **IT ALREADY DISCOVERS THEM.** `git log -S` puts `_scanMatrix` (GHOST/RECLAIM) inside `checkUpkeep` since **V8.1** and `_checkParked` (PARKED_RESCUE/EVICT_PARKED) since **V8.10** — both present in `bf8aee34`, the very commit that created AUTOMATION_AUDIT.md, at `MatrixKeeper.sol:476-497`, a few lines below the `:465` that audit cites by number. Live Base Sepolia confirms it is CONFIGURED too: `configuredTierCount` 10, every `pairManagerForTier` set, 30 matrices reachable. **WHY checkUpkeep RETURNS ZERO ANYWAY:** 923 parked members and **not one past the 24h `parkedGracePeriod`** — ages 0→23.6h, median 1.2h, p75 8.0h, hard stop at the boundary. That is `copay_rescue.js` sweeping the queue the moment members cross 24h; everyone below is waiting their turn. The queue is clean (923 entries, 923 unique, 0 duplicates, 0 `parkedAt==0`, 0 stale) so no corruption hypothesis survives either. ~~**THE AUDIT'S "3 KEEPERS" ARE 1**~~ **— RETRACTED 2026-08-11. All three run.** I checked `crontab_v8_45_staggered.txt` in the repo instead of the VPS. Live `crontab -l`: `copay_rescue` `4-59/10`, **`fastlane_rescue` `3-59/10`**, **`evict_parked` `*/30`** (present on the VPS though absent from the repo's CryptoNova-Keepers folder). `route_rr.js` IS correctly trimmed, so that concern was unfounded. **REOPENED AND SETTLED — ✅ IMPLEMENTED 2026-08-11 (497 passing).** `fastlane.log` shows two zero-debt rescues at 00:03 (`$5.00 + $5.438759 >= $10.00`; `$12.50 + $14.76495 >= $25.00`) then THIRTEEN runs at zero. Self-funded members are **real but rare** — ~1/hour against ~900 parked, cleared within ten minutes, so both censuses sampled a dead stretch. The zero was CENSORING, not absence. Without an on-chain path those members wait 24h and are then given an SF loan anyway, because copay does not re-check self-funding after the wait. **SHIPPED:** `_checkParked` applies `parkedGracePeriod` only when `sfShare > 0`; otherwise a 300s race guard (`selfFundedGracePeriod`, governed, enumerated 0/60/300/900/1800/3600, matching fastlane's MIN_AGE). Eviction keeps the full window — no zero-cost version exists. `_triageParked` extracted for "Stack too deep" rather than enabling viaIR. **TESTS: `V8_48_SplitGrace.test.js` (11) against MOCKS** — the state is unreachable in a real fixture (members park at ~80% of fee, withdrawable freezes at park time, and 25 or 70 registrations give the same answer), so `contracts/test/MockKeeperScan.sol` sets the numbers the chain actually produced. Mutation-tested: collapsing the split back to one window fails 4 of them. The equivalence test now pins `selfFundedGracePeriod == parkedGracePeriod` EXPLICITLY rather than relying on the fixture never reaching the state — that implicit dependency is what made its first version worthless. **KEEPERS STAY LIVE AS BACKUP** (owner decision); races are harmless, reverting `"still in matrix"` inside performUpkeep's try/catch. MatrixKeeper 19,838 bytes, 4,738 free. **WHAT WAS BUILT AND REVERTED:** a split grace — apply `parkedGracePeriod` only when the rescue draws SF money, a 300s race guard (`selfFundedGracePeriod`, governed, enumerated) when the member funds their own re-entry, since `sfShare` is already exactly 0 in that case. Compiled, suite green at 486, `_triageParked` extracted for "Stack too deep" rather than enabling `viaIR`. **Reverted unshipped: 0 of 96 sampled parked members are self-funded.** It would have released nobody. **THE FINDING THAT OUTLIVES THE ITEM — AND THE CLAIM I FIRST OVERSTATED:** six parked members sit at **91-98% of the entry fee** (T1 $9.83/$10, T3 $47.18/$50, T4 $91.04-$92.35/$100). Effective contribution is `withdrawable + crossingReserve`, the reserve is a flat 50% of fee, so those six hold 41-48% of a fee in earnings and stop just under the 50% that would let them re-enter unaided. **CORRECTED 2026-08-11:** those six were not a sample — the census printed a member only when the ratio was **>= 90%**, capped at six, and I then wrote it up as "the system is a few percent away", which assumes a population I never measured. What is established is narrower: at least six members are within 2-9% of self-funding, and 0 of 96 sampled reach 100%. `scripts/diag_parked_truth.js` now records every ratio, prints percentiles and a histogram, samples evenly across each array rather than the two ends, and tabulates how many members each extra point of `CROSSING_RESERVE_BPS` would lift over the line. **Re-run it before sizing anything.** If the median is near the threshold this is the most valuable question in the backlog; if the six are a tail, it is a curiosity. **ALSO OPEN:** `route_rr.js` is scheduled `8-59/10` in `crontab_v8_45_staggered.txt` while AUTOMATION_AUDIT.md section D says it was trimmed 2026-08-06 and must not be re-enabled because it masked the `rescueReentry` bug — the repo file may predate the trim, but the live VPS `crontab -l` should be checked. | MatrixKeeper — no change | AUTOMATION_AUDIT.md, corrected |
| 24 | **Keeper policy != contract policy on frozen MatB.** `_isFrozenMatB` (MatrixKeeper:633) requires full AND (never rotated OR `frozenMatBTimeout` = **6 hours** stale). `frozen_matb_keeper.js` rotates the moment a MatB reads full (`occ=127/127 nextSlot=128`), i.e. within 10 minutes. The protocol has been running on the KEEPER's policy for 6,726 rotations, which is why the contract's own failure gate ("if this ever fires regularly, the routing design has failed", :453) never tripped — on-chain it almost never fires. Decide the real policy and make ONE layer own it. | MatrixKeeper | NEW, verified 2026-08-09 |
| 25 | ~~`adminForceRotateRoot` reverts at scale~~ **WITHDRAWN 2026-08-09 — NOT a live defect.** The "29%" was a LIFETIME ratio I presented as a current rate. Per-day distribution: 152 on 07-18, 90 on 07-19, **2,583 on 07-24 (91% of all of them, a single day)**, 2 on 08-02, and **nothing since**. 07-24 is the V8.44 go-live and 07-18/19 is the `occ=127/127 rot=0` signature the contract comments name — i.e. these are the incidents V8.44/V8.46 were built to fix, already fixed. The two most recent `ERROR` lines (08-03, 08-04) are QuickNode `50/second request limit reached`, not reverts. `frozen_matb_keeper.js` already guards with `staticCall` (:152) and `estimateGas` (:172). **Keep that guard when item 24 moves the trigger on-chain** — `checkUpkeep` has no equivalent, and that is the real carry-over. | — | withdrawn after verification |
| 12a | **Extract `MatrixKeeperLib`** (same pattern as TierRouterLib / MatrixLogicLib). MatrixKeeper has 535 free bytes — item 12 does not fit. This is a prerequisite task, NOT a deferral of 12. | MatrixKeeper | size baseline below |

| 26 | **SF L1 surplus redirect to CommunityWallet** (owner proposal 2026-08-09). Add `communityOverflowBps` (default **0** = no behaviour change at deploy) + `setCommunityOverflowBps` `onlyOwnerOrGovernance`. In `receiveLayer` layer==1 only: when `totalBalance >= sfTarget()` and `communityWallet != address(0)`, route `amount * communityOverflowBps / 10000` to CW via `ICommunityWallet.deposit` (SF must `forceApprove` CW first), remainder to SF as today. Add `totalRoutedToCommunity` + event. **Do NOT touch L3** — that overflow funds BuybackReserve, which supports the CNOVA floor (items 4/5/6). Evaluate `totalBalance` BEFORE crediting the deposit so the deposit cannot tip its own test. | StabilityFund (11,204 bytes free) | NEW |
| 27 | ✅ **IMPLEMENTED 2026-08-09** (owner: *"we will be redeploying before then, please make the necessary fixes to future proof it"*). `perGenesis = genesisTotal / COHORT_SIZE`, same for Pioneer; unfilled-seat share rolls over instead of concentrating. Contract got 22 bytes SMALLER (7,827 -> 7,805). New suite `test/V8_48_CohortSplit.test.js` — 4 tests, covering the exact live 500/146 state, per-head invariance across enrolment, every ramp point incl. an empty cohort, and no cross-cohort leakage. Full suite 444 passing / 0 failing, so nothing had encoded the old behaviour. **CommunityWallet had NO dedicated test file before this** — a contract holding member money, reachable only incidentally from two other suites, which is why the inversion survived. ORIGINAL: **cohort inversion — divide by `COHORT_SIZE`, not live count.** `distribute()` computes `perGenesis = genesisTotal / gCount` and `perPioneer = pioneerTotal / pCount` using LIVE lengths, so while the Pioneer cohort is partly filled each Pioneer out-earns each Genesis member. Live 2026-08-09: G=500, P=146 -> next distribution pays Genesis **$2.24** and Pioneer **$5.11**, 2.3x, on a wallet whose entire premise is rewarding early membership. Inverted for any Pioneer count below 334 (totalEnrolled < 834). Fix: divide both by `COHORT_SIZE` (500) so per-head value is fixed and unfilled seats simply roll over; keep `actualDist = perGenesis*gCount + perPioneer*pCount` so undistributed value stays in the pool. | CommunityWallet (16,749 bytes free) | NEW |
| 28 | **Distribution expiry silently forfeits member money.** A distribution expires after `distributeInterval` (30d) and `_sweepExpired()` returns it to the pool. Members are told nothing. Exposure is $0.81 today but the 2026-09-04 distribution will be ~$1,865. Decide: keep expiry (and surface it loudly), or remove it. If kept, the frontend MUST show claimable amount + days remaining, and `claimable(address)` (:373) already provides the number. | CommunityWallet + frontend | NEW |
| 29 | **`StabilityFund.sol:18` documents a carve rate that does not exist.** Header says *"L1: Per-entry stabilityBps carve (6% T1-T3, 5% T4-T10)"*. Deployed config is `SPLITS_ALL[4] = 300` bps — a flat **3% on all ten tiers** (`deploy_v8.js:103`, `tierSplits()` returns the same array for every tier). The comment is both tier-varying (it isn't) and ~2x the real rate. Modelling item 26 from it would have been 2x wrong. | StabilityFund (comment only) | NEW |

| 30 | ✅ **IMPLEMENTED 2026-08-09.** Deleted from PairManagerV8: `deployEntryThreshold`, `routeEntryThreshold`, `setEntryThresholds`, the `EntryThresholdsSet` event and `overflowActive()`, plus the dead `overflowActive` declaration in MatrixLogicLib's interface. **Verified before cutting: ZERO production reads of either value, no governance parameter wiring, no call from `deploy_v8.js` or `predeploy_check.js`** — the knobs were reachable only from the owner setter, and `overflowActive` only from tests. A tombstone comment replaces them explaining WHY a cumulative counter compared against a fixed number is the defect, so it is not reintroduced as a routing input. Tests: 36 call sites across 11 files. Two V8Elevator tests DELETED rather than retargeted (the setter's bounds/ownership check and the `overflowActive` view) — they asserted that a knob stores what you hand it and that a view agrees with the knob; neither describes member-facing behaviour and there is nothing left to point them at. One RETARGETED: `"routing no longer depends on routeEntryThreshold at all"` in V8_48_RescueRouting worked by setting the threshold to 1 vs 1,000,000 — no longer expressible, but `pair.totalRegistered` still exists and still must not steer routing, so it now compares a pair with real entry history against a nearly-empty one. **Frontend shipped with it** (see below). | PairManagerV8 | NEW |
| 31 | ✅ **IMPLEMENTED 2026-08-09.** `rescueReentry` routes a member already seated in the pair to `_freePairFor(member, fromPairIndex)` — the same call TierRouter:1382 makes. Seated in EVERY pair: calls `_forceExpand()` (try/catch) and retries, then fails loudly with `PM8: no seat available for duplicate` rather than stranding. Counters and `MemberRouted` credit the DESTINATION pair. Removes the mechanism TierRouter:1372 documents — *"a duplicate stops its pair dead the moment its holder reaches position 1"* — because rescueReentry is called with NO try/catch at MatrixLogicLib:773, so that revert took the whole cycle-out with it. ORIGINAL: **Double-entry members have no route to the next pair.** A member cannot hold two seats in one pair (V8.46 universal pair guard), and `rescueReentry` is called with NO try/catch at MatrixLogicLib:773 — so when a double reaches MatB position 1 the re-entry reverts and takes the whole cycle-out with it. `TierRouter:1372` already documents the consequence: *"a duplicate stops its pair dead the moment its holder reaches position 1 (T3.1 and T4.1 both had to be repaired live on 2026-07-28)"*. Likely a large share of what `frozen_matb_keeper.js` force-rotates 6,726 times. Fix: mirror TierRouter:1382 — `freePairFor(member, fromPairIndex)`. **OPEN DECISION:** when the member is seated in EVERY pair (`freePairFor` returns `uint256.max`), park them in the matrix they cycled out of (keeps the pair turning, strands nobody, existing rescue machinery handles parked members) vs revert (wedges the pair) vs skip silently (strands the fee behind a live approval). Owner recommendation pending. | PairManagerV8 | NEW |

| 32 | **Backup keeper: detect `PM8: no seat available for duplicate` and spawn a pair.** The contract already tries `_forceExpand()` itself (try/catch, so it can never revert a member's cycle-out), but a factory failure still leaves the member unseated. Owner: *"a member eligible to cross or enter a new pair should not be parked, a new pair should be spawned so they have space to sit."* Keeper watches for the revert and triggers expansion out-of-band. Should be unreachable — the 90% factory trigger keeps a standby pair open. **✅ PREMISE VERIFIED 2026-08-11:** revert string present at `PairManagerV8.sol:325` AND `:539` (the scope described one site; there are TWO — the cycle-out path and the registerFor duplicate route added this session). `_forceExpand()` at `:749` is genuinely failure-safe: `try IMatrixPairFactory(pairFactory).deployAndWire(...) {} catch {}`, so a factory failure cannot revert a member's cycle-out. The 90% trigger is real — `factoryExpandThresholdBps = 9_000` at `:126`. Both call sites need the keeper, not just one. | /root/keeper/ | NEW, premise checked |
| 33 | **Spawn a pair when the newest is FULL** (owner approved, not yet implemented). `_tryAdvancePair` fires on `newest.totalRegistered >= deployEntryThreshold` (cumulative — same family as 10b) or newest MatB >= 90%. Replace the cumulative trigger with `_pairFull(pairs.length - 1)`; keep 90% as the early warning that preserves deploy lead time. Makes `deployEntryThreshold` dead, joining `routeEntryThreshold` in item 30. | PairManagerV8 | NEW |
| 34 | ✅ **IMPLEMENTED 2026-08-09.** Four new tests in `V8_44_Overflow.test.js`, on a new `deployOnePairWithFactory` fixture (the existing O-fixture added its second pair BY HAND, so `_tryAdvancePair` never had a factory to call and neither trigger could fire). **O5** — the EARLY trigger: asserts the successor pair is deployed while pair 0 STILL HAS SEATS, which is the entire reason for keeping a 90% trigger. **O5b** — NEGATIVE CONTROL, identical drive with the threshold at 100%, proving occupancy crossing the threshold is what CAUSED the expansion rather than merely co-occurring with it. **O6** — the FULL-pair backstop. **O7** — the strengthened O4 the owner asked for: drives pure member-driven churn until pair 1 genuinely fills, then proves BOTH limbs of the routing rule from one event log (every member's first `MemberRouted` is pair 0; every routing INTO pair 1 belongs to a member already in the protocol), and NAMES THE ROUTE via `DoubleEntryFired` because `freePairFor` has two callers that emit the same event. Also locks the V8.46 universal pair guard under real duplicate volume: two pairs yes, both halves of one pair never. **FINDING, recorded not deleted: `fullTrigger` is SUBSUMED by `matBTrigger`.** `setFactoryExpandThreshold` caps at BPS_DENOM and occupancy can never exceed MATRIX_SIZE, so a full pair implies a full MatB implies 10000 bps — `_pairFull` can never be the SOLE cause of an expansion. It is defence in depth, earning its bytes only if occupancy drifts ABOVE MATRIX_SIZE (the V8.44 phantom-seat class, "occupancy drift +44"), where MatB reads 6/7 while the pair sums to full. **Do not reclaim those bytes under item 30's doctrine without reading this.** | test/V8_44_Overflow.test.js | NEW |

| 35 | ✅ **IMPLEMENTED 2026-08-09 — TWO ROUTING RULES IN ONE CONTRACT, AND THE VIEWS FOLLOWED THE WRONG ONE.** Found while wiring the tier card for item 30. `_findRoutingPair()` (V8.40: *"oldest pair whose MatA still has a free seat, else the newest"*) survived item 10b, which moved actual entry routing to `_findExternalPair()` — constant 0, ONE DOOR. **The two agree only while pair 0's MatA has room, and a FULL pair 0 is the designed steady state** — concentrating the front door is what holds it at MATRIX_SIZE and keeps it rotating. So the rule diverged precisely when the design was working. **Three PUBLIC VIEWS reported the stale rule as "the routing target": `getActivePair()`, `allPairsStatus().active[]`, `routingDistribution()` — and the member-facing tier card reads all three.** It would have labelled the empty standby pair "taking new entries", and because the home card selects which matrices to draw its seat bars from using that same flag, T1.1 could hold 254 members while the card drew T1.2's empty ones — the *"T1.2 reads 0/127 while the community fills T1.1"* bug the frontend comments already record, reintroduced from the contract side. **None of the three had a test.** Fix: `_findRoutingPair()` DELETED, all four call sites (incl. the legacy V7-compat `register()`) read `_findExternalPair()`. One rule, one place. Regression **O8** asserts all three views name pair 0 while pair 0's MatA is full, then registers a member to confirm the views agree with what the contract DOES — and recomputes the deleted rule inside the test, asserting it points at pair 1, so O8 proves it is standing on the divergence rather than on a state where both rules coincide (which is every state where pair 0 has room, i.e. most of them). **Fourth instrument in this project caught disagreeing with the mechanism it reports on**, after `set_entry_thresholds.js`'s routing-pair label, `diag_overflow.js`'s hardcoded pair index and the tier card's threshold copy. | PairManagerV8 | NEW, verified 2026-08-09 |
| 36 | **Frontend, shipped with item 30.** `index.html`: both threshold entries removed from `PM_ABI`; the `routeEntryThreshold().catch(() => 381n)` read in `_fetchStats` DELETED — after cutover that call reverts, the catch swallows it, and the card quietly reports a threshold of 381 that exists nowhere (a swallowed read impersonating a value, aimed at members). `renderTierPairHeader` loses its `threshold`/`deployThreshold` parameters; the threshold-derived `'saturated'` state label is gone; the standby-buffer prose is replaced by **seats remaining in the pair taking new entries** (owner decision 2026-08-09) — bounded at 254 and drawn from the same occupancy figures as the bars, so sentence and bar cannot disagree. Also removes **20 RPC calls per page load** on an endpoint we are already rate-limited on. Verified: all 5 inline script blocks parse, no orphaned variables, file ends `</body></html>`, 622,565 → 620,358 bytes. | CryptoNova-Testnet-App | NEW |

| 37 | ✅ **IMPLEMENTED 2026-08-10 (459 passing, +104 bytes).** `_credit()` now takes a `source` and a `payer` and emits `EarningsCredited(member, payer, source, amount)` at all eight credit sites. Five sources: DIRECT_ENTRY (the 2.5% carve), L1_REFERRAL, CHAIN_PAY, POOL_SHARE, ORPHAN_ACCT1. **The `payer` field is what makes it answer the real question** — an L1 or chain-pay credit names the member whose ENTRY generated the payment, so a member sees not just "$2.50, referral" but WHO paid them. `ChainPayDistributed` and `PoolShareCredited` DELIBERATELY KEPT (frontend + VPS keepers read them; retire only after the frontend migrates). Cost 104 bytes in MatrixLogicLib — FigureEightMatrixV8 and MatrixPairFactory unchanged, which is the item 1 library move paying off again. **Test `V8_48_EarningsSource.test.js` (3), with an honest limit:** it verifies the two previously-SILENT paths (direct earn, L1) now emit and carry the correct source and payer. The headline sum-to-balance assertion is a DRIFT GUARD, not proof — `totalEarned` is incremented inside `_credit` one line from the emit, so they cannot disagree today; it catches a FUTURE path that credits without going through `_credit`. **NOT yet verified:** source labels on CHAIN_PAY, POOL_SHARE and ORPHAN_ACCT1 — those need rotations and orphaned entries the fixture does not produce. **STILL TO SHIP: the frontend half.** The event exists and nobody sees it until the dashboard groups the breakdown by source; `QA_EARNINGS_ANSWERS.md` currently tells the team a source breakdown is impossible and must be updated when that lands. ORIGINAL: **EARNINGS SOURCE TRANSPARENCY — members cannot see WHY they were paid** (owner decision 2026-08-10: *"I would like to have full transparency and if they want it and it is cheap why not"*). `_credit()` (MatrixLogicLib:~1030) increments `withdrawable` + `totalEarned` and **emits NOTHING**. Two of the four earning paths are therefore invisible to any consumer: the **2.5% direct earn** on entry (`DIRECT_EARN_BPS`, :805) and the **L1 referral payment** (:811). Chain pay (`ChainPayDistributed`, :964) and pool share (`PoolShareCredited`) DO emit, so the dashboard can attribute those two and only those two. **The failure case is observable while the success case is not:** an ORPHANED L1 emits `OrphanFeeRouted`, but an L1 that actually pays a referrer is silent. Consequence today: the member dashboard's breakdown is PER-TIER, not per-source — a member sees their balance rise with no way to learn whether it came from a referral, from someone entering below them, from the pool, or from their own crossing. **FIX:** add `uint8 source` to `_credit` and emit `EarningsCredited(address indexed member, address indexed payer, uint8 indexed source, uint256 amount)`. Eight call sites to tag (MatrixLogicLib :500 pool share, :805 direct entry, :811 L1 referral, :894/:913/:934/:936 accountOne + orphan fallbacks, :964 chain pay). **KEEP `ChainPayDistributed` and `PoolShareCredited`** — the frontend and the VPS keepers read them today, so removing them in the same release breaks live tooling; retire them only once the frontend has migrated to the unified stream. Cost is small: MatrixLogicLib has **3,072 bytes free**, an indexed event plus eight emits is well inside it — but run `sizes.js` after, MatrixLogicLib is the second-tightest contract in the system. **Ships with a frontend change** that groups the dashboard breakdown by SOURCE as well as by tier; without that the event exists and nobody sees it. | MatrixLogicLib + frontend | NEW 2026-08-10 |

| 38 | **CODE↔FRONTEND PARITY AUDIT — make it a deploy gate** (owner rule 2026-08-10: *"code is truth but should also be reflected on the frontend/website"*). Three divergences were found in a single session, all member-facing, none caught by 452 passing tests — because the tests verify the CONTRACT and nothing verifies that the SITE says the same thing. (a) the tier card told members a standby pair opens at N entries, reading a knob that steered nothing, behind a `.catch(() => 381n)` that would have survived the knob's deletion and printed a stale 381 for ever; (b) the card named T1.3 as "taking new entries" while every new member entered T1.1, because it read `allPairsStatus().active[]` — computed from `_findRoutingPair`, a DIFFERENT rule from the `_findExternalPair` registrations actually use (item 35); (c) the earnings breakdown groups by tier and cannot group by source, because `_credit()` emits nothing for L1 or direct-earn (item 37). **DELIVERABLE:** `PARITY_AUDIT.md` — one row per member-facing claim on the site, the contract function/constant that backs it, and a verified date. Anything without a named source does not ship. **Add to `scripts/predeploy_check.js`** the mechanical half: fail the gate if `index.html` references any contract member absent from the deployed ABI (catches exactly the item-30 class, where the frontend outlives a deleted getter). The judgement half stays a human read of the checklist. | frontend + scripts/predeploy_check.js | NEW 2026-08-10 |
| 43 | ✅ **IMPLEMENTED 2026-08-13 (540 passing) — THE PROPOSAL FEE, MADE REAL (owner request 2026-08-13: "introduce the fee on contract for submitting proposals, DAO votable").** History, verified this session: **the fee never existed on chain.** V8.34 shipped the FRONTEND for a "100 CNOVA burned on propose" fee — and V8Governance.test.js's fixture has carried a MaxUint256 approve "for the 100 CNOVA proposal fee" ever since — but the contract half was never built; `proposalFee()` did not exist, the UI's read always reverted, and a `.catch` dressed the revert as 100e18 until the 2026-08-07 audit removed the fiction (governance.html:1749 documents it). This item completes what V8.34 started. **DESIGN (owner-decided via picker 2026-08-13): burn CNOVA · default 100e18 · menu [0, 2.5, 5, 10, 25, 50, 100, 250, 500, 1000]e18.** `PARAM_PROPOSAL_FEE = 58` (self-param, target address(0)), `proposalFee` declared default (item-42 doctrine: nothing to run post-deploy), charged by BOTH `propose()` and `proposeBoostTable()` via `_chargeProposalFee` — allowance-path `burnFrom` (governance holds no BURNER_ROLE; the proposer consents with one approve; vest-locked CNOVA burns fine and the ledger follows, items 8+9), fee AFTER all validation so pre-fee errors surface without an allowance, `ProposalFeeBurned(id, proposer, amount)` emitted. **0 is on the menu deliberately** — the escape hatch; a default absent from the menu can never be voted back (item-42 lesson). Voting untouched. **TESTS `V8_48_ProposalFee.test.js` (6):** PF1 burn is real (balance, totalSupply and totalBurned all move by exactly the fee); PF2 no-allowance and can't-afford both revert with the token's own errors and create nothing; PF3 the array path pays too; PF4 menu discipline verbatim incl. default-on-menu and 0-on-menu; PF5 the fee governs itself — full DAO lifecycle votes it to 0 and the next proposal burns nothing; PF6 voting free is PINNED (castVote with zero allowance, balance unchanged). Existing suite unaffected — the V8.34 fixture approve finally does its job. **FRONTEND HALF: ✅ SHIPPED (`32f719d`) AND PROMOTED TO `main` 2026-08-13 (`41aaa2e`), probe re-run first — `proposalFee` MISSING on V8.47 and feature-detected exactly as designed:** fee read via retryRead, static "no CNOVA fee" notice replaced by the live figure, allowance check + exact-fee approve step before propose, param 58 in the picker/PARAM_FALLBACK. NOTE for deploy_v8.js review: no wiring needed (declared default), but the fee makes governance.html's leftover ERC20 approve/allowance ABI lines live again. | V8Governance | NEW 2026-08-13 |
| 44 | ✅ **SHIPPED TO `admin` 2026-08-13, PROMOTED TO `main` same day (`41aaa2e`, probe gate passed) — governance param UX, two member requests via the owner:** (a) **plain-language explanations**: every PARAM_FALLBACK entry gained a `plain:` field (55 params), surfaced as a "💡 What this means" box the moment a param is selected in Create Proposal (scalar AND array paths) AND on every proposal card under the param label — voters see what they are voting on, not just "Quorum BPS → 300". i18n-ready via `t('govPage.params.pN_plain', fallback)`. (b) **param search**: live search box above the picker; index built FROM the live `<select>` optgroups + PARAM_META at first use, so search can never disagree with the picker (one source of truth — the item-35 doctrine); matches label/plain/hint/group/#id, click loads the param. Checks run: node --check on all 4 inline script blocks, ends `</body></html>`, new ids unique. **Also verified this session (owner question "the fee seems gone"):** voting is free on-chain (castVote reads balance, moves nothing) and the proposal fee's disappearance from the site was the 2026-08-07 audit REMOVING a fabricated number, not a regression — see item 43 for the real fee that replaces it. | CryptoNova-Testnet-App governance.html | NEW 2026-08-13 |
| 45 | **DIAG NEEDED: 16 persistent parked-queue GHOSTS** — found 2026-08-13 in copay.log: the same 16 wallets revert `F8V8: already in matrix` on EVERY copay run (including the keeper signer 0xd419681B, T2.1 MatB), and diag_parked_truth's past-grace examples are the same wallets (0xd419681B 28.2h, 0x9179a58a 32.4h). State: `isParked() == true` in the origin matrix while seated somewhere — parked AND in-matrix simultaneously, so every rescue attempt reverts and the queue slot never clears. The 2026-08-11 "queue clean, 0 stale" claim is stale. ✅ **DIAG DONE 2026-08-13 (`scripts/diag_ghost_parked.js`, full-queue census + per-ghost tx attribution) — FIX TO BUILD with 46/47.** **MEASURED: 41 ghosts** (grew from copay.log's 16), of 992 live queue entries / 951 clean parked / 0 zero-parkedAt residue. **39 of 41 are ONE pattern: parked in a MatB, later seated in the SAME PAIR's MatA** (T2.1B→T2.1A, 9× T3.1B→T3.1A, 23× T4.1B→T4.1A, 3× T5.1B→T5.1A) — the original parkedAt SURVIVES the seat because enterMatrix's V8.46 dequeue (:331) is MATRIX-LOCAL by design and every MatB rescue destination is the pair's MatA (item 10). Seating txs: 29 during coPayRescue keeper runs, 5 adminForceRotateRoot (deployer 0xCd0Af6), 4 member selfRescue, 1 performUpkeep, 1 bulkUpgrade. (Attribution = the tx containing the ghost's MemberEntered; in a keeper tx the member being rescued may be someone else whose entry CASCADED the ghost into the seat — the fix does not depend on that distinction.) The other 2: SEATED-BEFORE-PARKED, cross-PAIR (parked T3.1A while seated T3.2A — the item-31 freePairFor duplicate route; the park sites :443/:753/:780/:810/:837 check only their own matrix). One tx (0x47f71338…) seated ghosts in T4 AND T5 — an upgrade cascade seats across tiers in one tx. Keeper signer 0xd419681B and the deployer are both ghosts (test wallets; no member harm — every ghost holds a live seat and their funds). **FIX (ships in the 46/47 package): (a) PREVENTION — taking a seat also clears the PARTNER half's park record** (the V8.46 pair guard already cross-calls the partner at :307, so the plumbing pattern exists; kills the 39/41 class at the source). **(b) Residual classes (cross-pair, park-while-seated) are cleaned by item 47's valve: a ghost is DEQUEUE-ONLY — never the fund-return/eviction path** (they are seated and earning). Item 46 must not count a ghost as a stranded borrower. ✅ **FIX IMPLEMENTED 2026-08-13 (with 46/47, 553 passing):** enterMatrix now partner-clears via new `clearParkRecord(member)` (partner-gated in the lib, `GhostDequeued` emitted; try/catch by design — residue cleanup must never cost a member their seat). GF-P1 drives the exact live shape (parked in MatB, seated into MatA through an impersonated partner crossing) and asserts the origin record dies with the seat. NOTE: the SAME-PAIR ghost class is what prevention kills; a CROSS-PAIR ghost (the 2 live seated-before-parked cases) falls to the valve's EVICT branch — dequeue + reserve release in the stale pair, seat in the other pair untouched — which is cleanup plus a refund, deliberately acceptable. | MatrixLogicLib / FigureEightMatrixV8 / MatrixV8Interfaces | MEASURED + FIXED 2026-08-13 |
| 46 | ✅ **IMPLEMENTED 2026-08-13 (553 passing).** `StabilityFund.insolvencyFloorBps` (declared default **3400** = the measured ~34% median per-cycle earnings), `loanEligible(member, tierIdx)` view, hard enforcement in the two lending functions — **`payCoRescue` and `payForceCross` are now MEMBER-AWARE** (a lender that does not know who it is lending to cannot refuse anyone; the old member-less signatures are gone, rippling through MatrixV8Interfaces, MatrixKeeperLib's interfaces, MockKeeperScan/MockRescueMatrix/MockEvictMatrix and two old suites' direct calls). Reverts `"SF: insolvency floor"` — greppable in copay.log. **PARAM_SF_INSOLVENCY_FLOOR = 59**, menu `[0, 1700, 2500, 3400, 5000, 6800, 10000]` (0 = disabled, the escape hatch; default and 0 both on the menu — GF-G1 pins it). Keeper discovery routes floored members to the valve (GF-D1) — and a floored but SELF-FUNDED member still routes to RESCUE (GF-D2): the floor gates LENDING only; `selfRescue` is deliberately ungated and GF-F4 proves a hard-floored member can always pay their own shortfall and re-enter. Unregistered tier (fee 0) cannot floor anyone. **FRONTEND HALF STILL TO SHIP (parity rule): the dashboard must SAY "no loan — over the insolvency floor, eviction follows unless you self-rescue"** — post-deploy task with the other V8.48 frontend switches. **VPS NOTE for deploy day: copay_rescue.js will now log `SF: insolvency floor` reverts for floored members — that is the floor working, not an outage; the on-chain valve drains them.** ORIGINAL: **BUILD (owner-decided 2026-08-13, package with 47): INSOLVENCY FLOOR on SF rescue loans** — no new loan when the member's existing debt already guarantees the next shortfall (debt ≥ expected next-cycle earnings; exact on-chain predicate to design — the diag used ~34% of the member's loan-tier fee). Today's cohort: 64 wallets, $1,917, 26% of the book. **OWNER POLICY, his words 2026-08-13: "evict them when their fees are maybe more than the fees they are collecting… they will be evicted and would need to pay the full fee to get back in. It is not a free ride for ever — the idea is to help them, but if they are not getting any referrals they would be evicted bcuz the loan no longer covers their coverage %."** I.e. the floor predicate IS the eviction trigger: once debt outruns per-cycle earnings, lending stops and item 47 evicts — member keeps their withdrawable, loses the position, re-entry costs the full fee; referrals are the designed way to stay solvent (FUNDING CONSTRAINT row + model_reserve_bps both confirm no parameter tweak saves this cohort). Design must answer: where the check lives (StabilityFund vs MatrixLogicLib rescue paths), the predicate's earnings estimate, ghost exclusion (item 45 — a ghost is not a stranded borrower), and the member-facing surface (the dashboard must SAY why no loan came and that eviction follows — parity rule). | StabilityFund / MatrixLogicLib | NEW 2026-08-13, policy decided |
| 48 | ✅ **IMPLEMENTED 2026-08-13 (owner request: "change the stability fund per tier multiplier params from 20 to 10 across the board").** Declared default in the StabilityFund CONSTRUCTOR: flat 20x (V8.26) → **flat 10x**, all tiers. Item-42 doctrine verified before editing: `deploy_v8.js` never sets `sfTargetMultiplier`, so a fresh deploy takes the constructor value and there is nothing to run post-deploy; and **10 is on the PARAM_SF_MULT_T1..T10 governance menu** ([5,10,15,20,30,40,50,75,100,150]) — as is 20, so the decision is reversible by vote either way. Also corrected the stale :156 doc comment still describing the never-shipped V8.21 "x10/x20/x30 ladder". **CONSEQUENCES (stated in the constructor comment):** `sfTarget()` halves at every tier → `healthBps()` reads 100% at half the old balance → the L3 withdrawal-fee slider tips toward BuybackReserve earlier, item 26's surplus redirect arms earlier, and `setStabilityFloor`'s cap (floor <= target) halves. **Tests:** 9 pinned sites in `V8Governance.test.js` updated (defaults test, `sfTarget()` auto-compute now fee x10 — $100/$150 not $200/$300, the floor-bound test rescaled), plus a NEW default-on-menu assertion (the item-43 lesson: a default absent from its menu can never be voted back). Suite: V8Governance 43 passing / 0 failing after the change (run in isolation; full-suite rerun pending with the next contract item). **Other consumers verified clean by grep:** `system_keeper.js` and `status.html` read `sfTarget()` live off the chain (status.html's hardcoded $200 was already removed by the 2026-08-07 audit); no keeper, script or frontend hardcodes the 20x. | StabilityFund / V8Governance.test.js | NEW 2026-08-13, owner decision |
| 47 | ✅ **IMPLEMENTED 2026-08-13 (553 passing).** `MatrixLogicLib.evictParked` is now the TWO-BRANCH VALVE: **(ghost — seated in self OR partner)** dequeue-only, `GhostDequeued(member, staleParkedAt)` emitted, funds and seat untouched — the pre-V8.48 `require(!isInMatrix)` REVERTED here, which is precisely why ghosts were uncleanable by any path, so do not "restore" it as a safety check; **(genuinely parked)** dequeued + crossing reserve RELEASED to withdrawable in full (`EvictionReserveReleased`) — involuntary exit, no exitSeat penalty; SF debt stays booked and repays off the top of the next withdrawal, which is what finally drains the book; re-entry costs the full fee (owner policy verbatim in the item-46 row). `MatrixKeeper._doEvictParked`: ghosts BYPASS the rotation + 7-day gates (dequeue is harmless at any age); real evictions keep both V8.46 gates, and performUpkeep stays allowlisted. GF-V3 drives the real reserve-bearing state (MatB root shortfall cycle-out via `adminForceRotateRoot` — a MatB-only tool, the first fixture aimed it at MatA and the contract said so) and pins the release to the cent. **KEEPER STORY, decided by this design: the broken VPS `evict_parked.js` is now REDUNDANT — on-chain discovery routes evictions itself (floored → EVICT after grace, ghosts → EVICT immediately after grace). Delete the cron line at deploy; do NOT fix its guard.** ORIGINAL: **BUILD (owner-decided 2026-08-13, package with 46): EVICTION RELIEF VALVE that actually fires** — zero evictions have EVER fired. On-chain policy exists (`rescueRatioBps = 7000`) but copay clears the boundary before on-chain discovery runs, and the VPS evict keeper has never run (self-matching pgrep guard) AND is unsafe (borrows the matrixKeeper slot, skips the rescueRatioBps check — standing warning). Design the valve in CONTRACT code with TWO branches, per the item-45 measurement: **(ghost) member seated anywhere in the pair → DEQUEUE-ONLY**, clear the stale record, touch no funds, no seat; **(insolvent, item 46 floor tripped, owner policy above) → evict**: exit the queue with withdrawable intact (V8.33 reserve-return rule), position gone, full fee to re-enter. Coordinated with item 46 so floored members drain rather than accumulate. Decide the keeper story afterwards — the broken cron guard is NOT the fix. | MatrixKeeper(Lib) / MatrixLogicLib | NEW 2026-08-13, policy decided |

| 39 | **"WHY HAVEN'T I CYCLED?" — the dashboard shows cycles but never shows DISTANCE TO THE NEXT ONE.** Two members have now reported this independently: Sherwyn 2026-08-08 (6 cycles vs 1) and CryptoJan22 2026-08-10 (*"this account has 5 directs... shows that it cycled only 2 times... i figured there would have been more cycles"*). **Neither is a defect — both are the interface failing to teach the mechanism.** Cycles come from `_cycleOutRoot` advancing every member one seat toward position 1 on each rotation; **direct referrals do not affect cycle rate at all.** What sets it is (a) the seat you were given and (b) whether your matrix is still receiving entries. Members have no other model available, so they reach for the one number they can see — their directs. Diagnosed live for CryptoJan22 (`0x79470c63…`): T1.1 MatA **seat 88/127**, 3 cycles, **87 rotations from cycling out**, in the pair that IS receiving entries (630 rotations) — i.e. perfectly healthy and roughly a day and a half away at the observed ~62 rotations/day. `diag_cycle_rate.js` prints that in one line; the dashboard cannot. **✅ SHIPPED 2026-08-11 (frontend, no deploy).** Each active position now reads `seat N of 127 · K rotations until you cycle · about T at the recent rate (R/day)`, and seat 1 reads *"you are next to cycle out"*. The RATE has no on-chain source — `rotationCount` is a running total with no history — so the dashboard SAMPLES it: `(count, timestamp)` per matrix in localStorage, rate computed from the oldest sample held, samples older than a week dropped, a negative delta (redeploy reset the counter) discards the history rather than reporting nonsense, and a first visit says *"measuring this matrix's rate…"* instead of inventing a number. **Cycle reads de-fabricated:** `tierCycles` at the headline and the tier-detail rows now use `retryRead`, so a failed read shows `12+ ⚠ retry` and `T3: ?` instead of a confident `0` — and an unreadable tier no longer falls into the "referral commission — not joined" branch, which was a confident wrong explanation built on a dead RPC call. **STILL OPEN (deliberately not changed late in a session):** `index.html:4147` and `:4929` read `tierCycles(...).catch(() => 0n)` as UPGRADE GATE logic — a failed read there reads as "0 cycles" and can wrongly LOCK a member out of an upgrade. Different failure mode from a wrong display, and it needs its own UI state ("could not verify") rather than a silent default. ORIGINAL FIX: surface seat position and rotations-remaining on the dashboard — `matrixPos` is ALREADY read by the frontend so the data is in hand and unused — **line refs corrected 2026-08-11: `index.html:4982/7334/7405`** (ABI at `:2289`). The old 4930/7282/7353 drifted ~52 lines when index.html changed earlier this session; verify by grep, not by line number. Add "seat N of 127 · M rotations until you cycle" plus the matrix's recent rotation rate so the number means something in time. **ALSO REVIEW while there:** (a) the cycles read is behind `.catch(() => 0n)` — **corrected and EXPANDED 2026-08-11: there are more than the three listed.** `tierCycles` is caught at `index.html:4074/4856/5778/6227` (FOUR sites, not three) and `rotationCount` at `:7328/7423` (two more, same defect: an RPC hiccup renders a confident zero). All six must be handled, not three — an RPC hiccup renders "0 cycles" to a member who has cycled, the same swallowed-read class as item 30's tier card; (b) the locked-feature hint at `:4049` explains a cycle as *"MatA fills (126 members), then MatB fills (127 members)"*, which is not the rotation mechanism the contract implements and is a candidate for the same misunderstanding. | frontend | NEW 2026-08-10, from BUGS.md |

| 40 | ✅ **CONTRACT HALF IMPLEMENTED 2026-08-13 (556 passing) — AND THE LAST UNVERIFIED EXTERNAL CLAIM IS NOW VERIFIED.** `selfRescueWithPermit(value, deadline, v, r, s)` — body in MatrixLogicLib (`_selfRescue` extracted, both externals share it; the matrix wrapper cost the factory 184 bytes, headroom 532→348), mirroring `TierRouter.manualUpgradeWithPermit` exactly including the try/catch (a griefed/pre-consumed permit must not brick a rescue whose allowance already stands; a bad permit with NO allowance surfaces the token's own `ERC20InsufficientAllowance`, never a silent pass). `IERC20PermitLike` added to MatrixV8Interfaces. **Tests `V8_48_Permit.test.js` (3):** PW1 zero-pre-approval one-tx rescue (allowance asserted 0 first, wallet debited exactly the shortfall); PW2 the griefing scenario end-to-end (permit consumed by a third party, rescue still lands); PW3 loud failure. **THE CLAIM: `scripts/probe_base_usdc_permit.js` (run 2026-08-13, Base mainnet 8453): native USDC 0x833589fC… IS EIP-2612 — version 2, DOMAIN_SEPARATOR answers, nonces() answers.** Nothing about mainnet blocks the permit UX. NatSpec gotcha recorded: an `@handle` in a `///` comment parses as a doc tag and fails the compile — write member names without `@`. **POST-DEPLOY frontend switch (with the item 2/3/43 batch): clear-all should sign a permit per position (one tx each) instead of approve+selfRescue — feature-detect `selfRescueWithPermit` on the deployed matrix.** ORIGINAL: **ONE-CLICK "CLEAR ALL" FOR MULTIPLE PARKED POSITIONS** (member request via QA, 2026-08-10). A member parked in more than one matrix must today approve and Self Rescue each one separately — 2 positions = 4 wallet transactions for a total of **$4.28**. The panel already computes and shows the total correctly; what is missing is a single action. **WHY ONE APPROVAL CANNOT COVER BOTH:** `selfRescue()` takes no arguments and spends the member's ERC-20 allowance granted to THAT MATRIX, so each position has a different spender. There is no multicall contract in the system, and a batch helper cannot work because `selfRescue` derives the member from `msg.sender` — a helper would rescue itself. **SHIP NOW (frontend only, no deploy):** one `Clear all (N) — $X.XX` button that sequences approve->selfRescue per position, plus `Copay all — borrow $X.XX` for the loan route. MUST re-read state after each step and report partial completion honestly ("cleared 1 of 2, T3.1 failed: <reason>") — a clear-all that reports success it did not verify is the same defect class as the top-up that printed a landed transfer while the balance was unchanged (bigfill, 2026-08-10). **SHIP WITH V8.48 (halves the transactions):** add `selfRescueWithPermit(uint256 value, uint256 deadline, uint8 v, bytes32 r, bytes32 s)` mirroring the existing `TierRouter.manualUpgradeWithPermit` (:910). MockUSDC already has ERC20Permit (V8.44 G1 — **verified 2026-08-11** in `contracts/test/MockUSDC.sol:18`, and that IS the artifact hardhat builds for the name `MockUSDC`; `contracts/MockUSDC.sol` is a deliberate 5-line tombstone pointing at it) and native USDC on Base supports EIP-2612 — **⚠ the ONE claim in these three items that could not be verified from source: it is about a live external contract. Check `DOMAIN_SEPARATOR()`/`nonces()` on Base USDC before relying on it.** `selfRescue()` taking no arguments is confirmed (`FigureEightMatrixV8.sol:567`) and `TierRouter.manualUpgradeWithPermit` is exactly at `:910`, so the approval becomes a FREE off-chain signature and each position costs ONE transaction instead of two — 2 positions goes from 4 transactions to 2. **LATER:** EIP-5792 `wallet_sendCalls` would make it a true single confirmation, but wallet support is uneven; needs the sequenced path as fallback regardless. | frontend + FigureEightMatrixV8 | NEW 2026-08-10, member request |
| 41 | ✅ **IMPLEMENTED 2026-08-10.** **DISTRIBUTION IS NOW THE 25TH OF EVERY MONTH, IN THE CONTRACT.** Owner: *"it should be the 25th of every month but it says 25 days not the 25th"* — correct, and the belief traced to a `day-of-month >= 25` gate that lived in the FRONTEND until 2026-08-07 and that the contract never had. `distributeInterval` DELETED (a rolling 30-day window measured from the last actual run, drifting ~5 days a year: 4 Sep, ~4 Oct, ~3 Nov). Replaced with `distributionDayOfMonth` (default **25**, governed, capped at **28 so the date exists in February** — 29-31 would silently skip short months), `lastDistributionMonth` (year*12+month, guaranteeing AT MOST ONE run per calendar month — a day check alone would let the 26th fire a second one), Hinnant `_civil`/`_fromCivil`, and **`nextDistributionTime()` as THE single source of truth the frontend reads**. `expiresAt` is now the NEXT monthly date, not +30 days (a January run now expires 25 Feb, not 24 Feb). `_gateAndExpiry()` was extracted rather than enabling `viaIR` — "Stack too deep" — so the contract is not left one local away from the same failure. **FOUR CONSUMERS WERE BROKEN AND ONLY ONE WAS OBVIOUS:** (a) `V8Governance` param 39 still routed to the DELETED `setDistributeInterval` — compiles, and reverts only when a PASSED proposal is executed; repointed to `PARAM_CW_DISTRIBUTION_DAY` → `setDistributionDayOfMonth`, allowed values `[1,10,15,20,25]`. (b) **`forceDistribute()` silently died** — it worked by zeroing `lastDistributionTime`, which `distribute()` no longer reads, so the testnet QA escape hatch reverted on any day before the 25th; nothing caught it because **no test had ever called it**. Now an explicit `_forcing` bypass, and it RESTORES `lastDistributionMonth` afterwards so QA on the 3rd does not cancel the real run on the 25th. (c) `predeploy_check.js` asserted the deleted setter. (d) `community_wallet_test.js` shortened the interval to 2 minutes and restored it — there is no interval left to shorten; it now uses `forceDistribute()` and VERIFIES the result instead of trusting the tx hash. **`community_wallet_test.mjs` moved to `_to_delete/`** — a strict subset fork of the `.js`, and the one that went stale. **Tests: `V8_48_Calendar.test.js` (13)** — asserts the DAY OF MONTH, never an elapsed-seconds delta, and reads dates back through an INDEPENDENT implementation (JS `Date`) rather than re-deriving Hinnant: twelve consecutive months incl. February and a year rollover all land on the 25th; the 26th and the 30th revert; `distributeReady()` tracks the calendar; the claim window ends on the next monthly date; and three tests pin `forceDistribute()`. **Deploy gate hardened while here:** `predeploy_check.js` pointed at a DEAD session mount (`/sessions/happy-amazing-curie/…`) and at `CryptoNova-App`, so **every frontend parity check had been silently SKIPPING** — exactly what the CODE↔FRONTEND PARITY rule exists to prevent. Path now resolves at runtime, a miss is a FAIL rather than a shrug, and both new checks were NEGATIVE-TESTED against the pre-fix content rather than assumed to work. | CommunityWallet / V8Governance / predeploy_check / index.html | Owner request 2026-08-10 |
| 41b | ✅ **IMPLEMENTED 2026-08-10 — the 65/35 modal, finally.** Long recorded below as "frontend modal says 65/35 — still wrong, separate". `index.html:6669` told members Genesis **65%** / Pioneer **35%** while `genesisBps = 6_000` pays **60/40**, and a second panel in the SAME file said 60/40 — the page contradicted the contract and itself. Corrected, and the member-facing copy now names the date ("the 25th of every month") in all four places instead of "monthly". **A number in prose is not checkable by the compiler or the suite, so a check was added:** `predeploy_check.js` now parses the `genesisBps` DEFAULT out of `CommunityWallet.sol` and compares it against every Genesis percentage claimed anywhere in `index.html`, failing on any disagreement. Negative-tested: it flags the pre-fix 65% wording. | index.html / predeploy_check | Owner parity rule |
| 12a | ✅ **IMPLEMENTED 2026-08-10 (486 passing).** **MatrixKeeper 24,041 → 19,479 bytes; headroom 535 → 5,097.** Item 12 could not be written into 535 bytes, so the read-only DISCOVERY path came out into a new linked `MatrixKeeperLib`: `checkUpkeep`'s scan, `_scanMatrix`, `_checkParked`, `_isFrozenMatB`, `_sfRescueBps`, plus the five keeper interfaces. **Execution deliberately stayed put** — `performUpkeep` and every `_do*` handler mutate state and move money; the point was headroom, not a rewrite of the parts with value at stake. The scan reads eighteen keeper variables, which now travel as ONE `ScanCfg` memory struct: passing them individually blows the stack in `_checkParked`, which already carried a comment about holding peak depth at 8. `lastGhostTime` is a mapping, so it crosses as a storage reference — libraries are the one place in Solidity where a storage pointer may cross an external call boundary. **PROVED BY EQUIVALENCE, NOT BY SCENARIOS.** `contracts/test/MatrixKeeperPrev.sol` is the pre-refactor keeper frozen at `7eaf3d6`; `V8_48_KeeperScan.test.js` (14) deploys both against one world and asserts `checkUpkeep` returns **byte-identical `performData`** across idle sweeps, batch truncation, every ladder preset, chain-link queues, the CW gate and the frozen-MatB backstop. **THE FIRST VERSION OF THAT TEST WAS WORTHLESS AND A MUTANT PROVED IT:** swapping `idleSlotTimeout` and `extendedIdleTimeout` in the snapshot SURVIVED all 13 green tests, because with only 7 registrations `matA.rotationCount()` is 0 and `_scanMatrix` returns on its first line — the idle sweep was never reached at all, while the file claimed to cover it. Fixed by registering 25 members and giving the timing-sensitive cases their own fresh worlds. **Harness strength is MEASURED, not asserted:** seven injected mutations, **four killed** (idle-threshold swap, dropped chain links, hardcoded maxItems, wrong communityWallet) and **three still survive** — all three recorded in the test header with the world state each would need. **Also found and fixed while here:** none of the three linked-library addresses (`MatrixLogicLib`, `TierRouterLib`, `MatrixKeeperLib`) were being written to the addresses file, and Basescan verification of a linked contract requires them — unrecoverable once the deploy transcript scrolls away. **The deploy gate earned its keep:** it failed on `distributeReady` the moment the CW trigger moved into the library, which is exactly what it is for. New gate checks assert `discover()` is `external` (an `internal` library function is inlined back and frees ZERO bytes), that the snapshot assigns every field, that the library is linked in `deploy_v8.js`, and that `MatrixKeeperPrev` never reaches a deploy script. **DELETE `MatrixKeeperPrev.sol` AND `V8_48_KeeperScan.test.js` WITH ITEM 12** — item 12 changes the scan on purpose, at which point the equivalence assertion is false by design. | MatrixKeeper / MatrixKeeperLib / deploy_v8 / predeploy_check | Prerequisite for item 12 |

## Deploy / script changes

| # | change | file |
|---|---|---|
| 13 | Call `setTier1Matrix` / `setMemberTracker` — never called, so `earlyExitPenaltyBps` returns 0 and `setFreeMode` (Universe Mode) reverts | scripts/deploy_v8.js | §7 |
| 14 | Assert every wiring setter was called; fail the gate if not | scripts/predeploy_check.js | §7 |
| 15 | Re-verify every ERC20 approval amount against what the release CHARGES | frontend + scripts/ + /root/keeper/ | §9 |

## Post-deploy actions

| # | action |
|---|---|
| 16 | ~~Revert the live mitigation~~ **WITHDRAWN — nothing to revert.** These are not proxies; V8.48 is a FRESH DEPLOY as V8.47 was on 2026-08-05. The `uint256.max` thresholds live on the OLD contracts and cease to exist at deploy. `routeEntryThreshold` is also no longer read by any routing path. |
| 17 | DELETE `route_rr.js` — **gated on 10b, NOT on 10.** It masks `_findExternalPair`, not `rescueReentry`. Deleting it after a 10-only fix re-freezes MatA in every mature pair — the 26 Jul state (T1.1=2129, T1.2=890, T1.3=101 vs threshold 381, two dead pairs). |
| 18 | **REVERSED 2026-08-09 — DO NOT RETIRE `frozen_matb_keeper.js`.** The log check was run and the earlier "redundant" call was based on MY bad grep (searched lowercase `rotated`/`forced`; the log writes `Rotated`/`forcing`, so 2 matches meant my pattern missed, not that the keeper was idle). Actual: **6,751 successful rotations, total_rotations=6726, runs=38445**, still rotating T1.1/T2.1/T3.1 every 10 minutes. Retiring it would freeze every MatB in the protocol. Replaced by item 24. |
| 19 | Retire `copay_rescue` / `fastlane_rescue` / `evict_parked` once #12 lands |
| 20 | Run the protocol's own gate: **keepers OFF -> rotationCount must still climb** (MatrixKeeper.sol:455) |

## Frontend (ships with, not after)

| # | change |
|---|---|
| 21 | Disable "Unlock early" / "Max Unlock" when `balanceOf < penaltyAmt` — currently offers an action that always reverts (§8) |
| 22 | Remove the interim self-rescue surplus warning once #11 ships |
| 23 | Restore the exit-penalty ladder copy once #7 makes it real |

## 10b IS NOT THEORETICAL — IT ALREADY FIRED

Timeline, established 2026-08-09 from the VPS crontab and `route_rr.js` state:

| when | what |
|---|---|
| 2026-07-27 | `route_rr.js` written to work around `_findExternalPair`. Live T1 was `[2129, 890, 101]` against threshold 381 — two pairs already dead. |
| 2026-08-06 | cron line commented out (`# TRIM-2026-08-06`). The mask is removed. Nothing else changes. |
| 2026-08-08/09 | **254 members frozen in T1.1 MatA.** Owner reports pair-A not rotating; member ticket (Sherwyn) says the same. |
| 2026-08-09 | Mitigated with `setEntryThresholds(_, 1000000)` — every pair under threshold, so pair 0 always wins. T1.1 MatA 316 -> 333 rotations, 254 members released. |

Three days between removing the workaround and the freeze. The mitigation treated
the symptom; `_findExternalPair` is the cause and was not identified until the
keeper source was read on 2026-08-09.

**The mitigation is not a resting state.** At `route=1000000` pair 0 takes every
new registration, so T1.2 and T1.3 MatA are now starved — live entry counts
`[3483, 595, 8]`. It traded a T1.1 freeze for a T1.2/T1.3 freeze. This is why
10b ships in V8.48 rather than living on as a threshold value.

**Current holding configuration (must not drift before V8.48):**
- `routeEntryThreshold = 1000000` (the mitigation)
- `route_rr.js` cron trimmed 2026-08-06 AND kill switch `route_rr.OFF` set 2026-08-09
- Item 16 reverts the threshold; item 17 deletes the script — both AFTER 10b ships

## THE ROUTING RULE — OWNER'S SPEC, STATED THREE TIMES 2026-08-09

Encode this and do not drift from it. Every wrong turn tonight came from violating it.

**NEW MEMBERS HAVE ONE ENTRY POINT. Never dilute new entries across pairs.**
`_findExternalPair()` returns 0. Always. Concentrating the front door is what holds pair 0
at MATRIX_SIZE and keeps it rotating — a full MatA only rotates when it RECEIVES an entry
(MatrixLogicLib:407), so diverting new members away from a full pair FREEZES it. That is
the 2026-08-06 incident, 254 members.

**EXISTING MEMBERS CYCLE, and that is what populates later pairs:**

| route | when | code |
|---|---|---|
| A -> B -> A **same pair** | default | `TierRouterLib.sameTierTarget` -> own MatA; `rescueReentry` -> own MatA |
| A -> B -> A **2nd pair** | member already holds a seat in this pair (V8.46 universal pair guard forbids two seats in one pair) | `_freePairFor(member, fromPairIndex)` |
| A -> B -> A **upgrade pair** | tier upgrade | TierRouter upgrade path |

Three attempts at `_findExternalPair` were rejected for violating the first rule, each
caught by a test rather than by reasoning: round-robin (dilutes; no pair reaches
MATRIX_SIZE so nothing rotates), share-across-full-pairs-plus-one-filling (dilutes),
divert-at-physical-fullness (freezes the pair it diverts from). **The answer was never a
routing formula for new entries — it was one door, plus giving CYCLING members the
2nd-pair route they were missing.**

## TOPOLOGY RE-VERIFIED FROM SOURCE 2026-08-09 (third pass)

Earlier in this work I diagnosed the pair topology wrong twice before landing on
`rescueReentry`. Re-read end to end rather than trusted from memory:

| path | code | threshold read? | status |
|---|---|---|---|
| cycle-out re-entry | `handleCycleOut` -> `_sameTierTarget` -> `TierRouterLib.sameTierTarget` | **no** | **CORRECT.** Own MatA unconditionally; `toMatB` only when the member already occupies own MatA. V8.46 DELETED `pairExpansionThreshold` and its setter. |
| rescue re-seat | `PairManagerV8.rescueReentry` :286 | **yes** — `routeEntryThreshold` | **BROKEN — item 10** |
| new registration | `PairManagerV8._findExternalPair` :578 | **yes** — `routeEntryThreshold` | **BROKEN — item 10b** |

Two survivors of the V8.46 root cause, not three. Nothing else reads a cumulative
counter to make a routing decision.

**Item 10 is ALREADY RUNNING LIVE, unintentionally.** `rescueReentry` compares
`totalRegistered >= routeEntryThreshold`, and the mitigation set that threshold to
1,000,000. No pair has a million cumulative entries, so the comparison is false
everywhere and **every rescued member is being re-seated into own MatA right now** —
exactly the behaviour item 10 makes permanent.

Evidence collected 2026-08-09 while running that way:

| metric | value |
|---|---|
| T1.1 MatA rotations | 316 -> 333 -> **374**, still climbing |
| T1.1 MatB occupancy | 126/127 — did NOT starve |
| T1.2 MatB occupancy | 127/127 — did NOT starve |
| frozen members released | 254 |

The one real risk in item 10 was starving MatB of its rescue feed. That risk is now
measured rather than argued: MatB stayed full while MatA resumed rotating.

**The collision guard is NOT optional.** `_sameTierTarget`'s comments record what
unconditional-MatA cost before V8.46-C: fork replay of graduation tx `0xff488549...`
(block 44702114) — member already held a T4.1 MatA seat, re-entry hit
`require(!isInMatrix)` at MatrixLogicLib:255, the empty catch at :513 swallowed it,
**9 events, 6 members, $467.50 lost.** Item 10 must mirror
`toMatB = isActiveInMatrix(member)`, not just "return MatA".

## WHY PAIR A ROTATES SLOWER THAN PAIR B — ANSWERED 2026-08-09

The owner reported on day two: *"members are not rotating fast enough in T1A or any
pair A as fast as the B pair"* and stated the intended design: *"when T1B cycles out
it should not go back to T1b it should go to T1A or T2A and continue up the ladder."*

Measured live across all 10 tiers (`diag_matb_freeze2.js`, paced + retried so no
failed read is mistaken for data):

| pair | entries | thr | MatA rot | MatB rot | ratio | `rescueReentry` target |
|---|---:|---:|---:|---:|---:|---|
| T2.1 | 5986 | 400 | 581 | 5684 | **9.8x** | MatB (saturated) |
| T1.1 | 3496 | **1000000** | 378 | 3210 | 8.5x (accrued BEFORE the mitigation) | **MatA** (mitigated) |
| T3.1 | 1153 | 400 | 434 | 870 | 2.0x | MatB (saturated) |
| T4.1 | 242 | 400 | 115 | 0 | — | MatA (under threshold) |

**Mechanism, confirmed.** `PairManagerV8.rescueReentry` :286 re-seats a rescued member
into own **MatB** once `totalRegistered >= routeEntryThreshold`. A member cycles out of
MatB, cannot fund the crossing, parks, is rescued — and is put back into MatB. A closed
loop. MatB churns, MatA crawls, and no one climbs the ladder. This is exactly the
behaviour the owner described from watching members, before it was found in code.

**The earlier hypothesis was WRONG and is recorded so it is not re-derived:** "MatA is
starved of all feed, so MatB gets no crossings and goes inert." T2.1 and T3.1 are
EXCLUDED by 10b yet their MatAs rotated 0.3h ago — cycle-out re-entry
(`sameTierTarget` -> own MatA) still feeds them. 10b's harm is that NEW members never
reach a mature pair (T2.2 has 35 entries, T3.2 has 3, while T2.1 has 5986); item 10's
harm is the MatB recycle loop. Two distinct defects, both real, both in scope.

**T1 is the control group.** It is the only tier carrying the mitigation
(`routeEntryThreshold = 1000000`), which forces `rescueReentry` down the MatA branch —
item 10's exact semantics. T1.1 MatA has gone 316 -> 333 -> 374 -> 378 today while its
MatB held 126/127. The fix is proven on live members before a line of it is written.

**Blast radius of 10b right now:** T2.1 (5986 entries vs threshold 400) and T3.1 (1153
vs 400) are permanently excluded from new registrations. T1 is masked by the mitigation.
T4-T10 are under threshold and therefore not yet affected — they WILL be, at 400 entries
each, with no code change required to trigger it.

## ITEM 10 MEASURED IN MEMBERS — PARKED CENSUS 2026-08-09 19:00 UTC

Rotation ratios say MatB churns faster than MatA. The parked census says who is paying
for it. Both halves, every tier (`parked_census.js`):

| tier | MatA parked | MatB parked | subtotal |
|---|---:|---:|---:|
| T1 | 24, 20, 0 | 62, 30, 0 | 136 |
| T2 | 69, 0 | **209**, 0 | 278 |
| T3 | 85, 0 | **165**, 0 | 250 |
| T4 | 50 | 0 | 50 |
| **TOTAL** | **248** | **466 (65%)** | **714** |

**466 members parked in MatB.** A member cycles out of MatB, cannot fund the crossing,
parks there — and `rescueReentry` at saturation re-seats them into that same MatB
(PairManagerV8:286). Two-thirds of everyone parked sits in the half that was recycling
them. This is item 10's cost expressed in people rather than rotation counts, and it is
the same defect the owner identified from member behaviour on day two.

**The rescue system itself is HEALTHY — verified, not assumed.** On-chain parked total
(714) reconciles EXACTLY with `copay_rescue`'s "714 still in grace" (86400s window).
Zero members are past grace awaiting a rescue that is not coming. `copay_rescue` runs
every 10 minutes, advances real USDC (3 rescued / $16.53 in one run), reports 0 failed;
`fastlane_rescue` and `evict_parked` are also scheduled. SF ~$10,900 and rising, ledger
conserving to the cent, fully backed.

Note for anyone re-deriving this: `rescue.log` is EMPTY and that is NOT a fault — no cron
writes to it. The live rescuers are `copay_rescue.js`, `fastlane_rescue.js` and
`evict_parked.js`. `rescue.log` is a stale log from a script no longer scheduled.

**Expected effect of the 2026-08-09 17:07 threshold change:** with every tier's
`routeEntryThreshold` unreachable, `rescueReentry` now takes the MatA branch everywhere,
so those 466 are re-seated into MatA as they are rescued rather than back into MatB. The
MatB share of parked members should fall. That is the metric to re-check tomorrow, and it
is the same behaviour item 10 makes permanent.

## COMMUNITY WALLET — VERIFIED STATE AND TWO FALSE BELIEFS 2026-08-09

Owner asked to verify two beliefs against code. **Both are false, and both match the
OLD FRONTEND's gating** — removed in Batch 1 (`b1ddefd`), which is where the mental
model came from. Recorded here so neither is re-derived.

| belief | reality (code) |
|---|---|
| "Genesis get paid when we hit 500, Pioneer at 1000" | **No cohort-fullness gate exists.** `distribute()` requires only `totalEnrolled > 0` (:262); it divides by LIVE `genesisMembers.length` / `pioneerMembers.length`. `claim()` requires only `cohort[msg.sender] != COHORT_NONE`. **A distribution already ran 2026-08-05** with G=500, P=146. |
| "Can claim once the 25th of the month hits" | **Was false when checked (2026-08-09); MADE TRUE 2026-08-10 by item 41.** At the time `distribute()` gated on `block.timestamp >= lastDistributionTime + distributeInterval` — a ROLLING 30-day timer that drifted each cycle, last 2026-08-05, next due 2026-09-04. It now gates on the CALENDAR: the 25th of every month, at most once per month. The owner's belief was ahead of the code, not wrong about the intent. `claim()` still has NO time gate — but each share EXPIRES on the next monthly date, so "claim whenever" is still the wrong reading. |

Verified live state:

| field | value |
|---|---|
| totalEnrolled | 646 / 1000 (Genesis **500**, Pioneer **146**) |
| split | 60% Genesis / 40% Pioneer (frontend modal said 65/35 — **FIXED 2026-08-10, item 41b**, and now guarded by a predeploy check) |
| distributeRatioBps | 5000 — 50% distributes, 50% rolls over |
| distributeInterval | 30 days (NOT 25, NOT day-of-month) — **DELETED 2026-08-10, item 41.** Now `distributionDayOfMonth = 25`. |
| lastDistributionTime | 2026-08-05T05:09:42Z |
| next due | **2026-09-04T05:09:42Z** |
| CW USDC balance | $3,730.94 |
| totalActivePending | $0.81 unclaimed |

**Payout model for item 26** (constants read from source, not assumed). Blended SF carve
**$1.08 per entry** at a plausible tier mix (T1 55%, T2 22%, T3 13%, T4 6%, T5 3%, T6 1%),
100% of L1 redirected while SF >= target, both cohorts full:

| members | cycles/mo | SF->CW /mo | Genesis ea | Pioneer ea |
|---:|---:|---:|---:|---:|
| 1,000 | 2 | $2,160 | $2.59 | $1.73 |
| 2,500 | 2 | $5,400 | $6.48 | $4.32 |
| 5,000 | 2 | $10,800 | $12.96 | $8.64 |
| 5,000 | 4 | $21,600 | $25.92 | $17.28 |
| 10,000 | 4 | $43,200 | $51.84 | $34.56 |

Linear in members x cycles x redirect fraction. **Be honest in the announcement copy:**
at today's scale this is a few dollars a month; it only becomes material at 5-10k active
members.

**The redirect is self-limiting, and the DAO dial already exists.** `sfTarget()` =
`tierEntryFee[highestOpenTier-1] * sfTargetMultiplier` (currently **20**). Target ladder:
T5 $5,000 -> T6 $10,000 -> T7 $20,000 -> T8 $50,000 -> T9 $100,000 -> T10 **$200,000**.
As high tiers open the SF must hold far more before a cent redirects, so payouts throttle
naturally. `setSfTargetMultiplierT1..T6` are ALREADY `onlyOwnerOrGovernance` — that
multiplier, not a new threshold, is the lever the DAO votes on. Lower = more to members,
higher = harder fund.

**Current SF surplus:** target $5,000, balance $10,929.31, **$5,929.31 idle**, healthBps
maxed at 10000. Owner decision 2026-08-09: **leave it** — T6 opening moves the target to
$10,000 and T7 to $20,000, so growth absorbs it, and no privileged sweep function needs
to be written or audited.

**Why the existing overflow does nothing:** lifetime `totalRoutedToBuyback` = **$6.88**
and `totalRoutedToSF` = **$0.77**. The entire sliding mechanism has governed $7.65 while
$10,929 accumulated through L1/L5, which have no ceiling. The design is sound but attached
to the wrong layer.

## SIZE BASELINE — measured 2026-08-09, before any V8.48 code

Deployed-bytecode size vs the EIP-170 limit of 24,576 bytes.

| contract | bytes | free | touched by | verdict |
|---|---:|---:|---|---|
| TierRouter | 24,434 | **142** | 2, 3 | **BLOCKED** — must land in TierRouterLib |
| MatrixPairFactory | 24,220 | **356** | — | leave alone; no scope item touches it |
| MatrixKeeper | 24,041 | **535** | 12 | **BLOCKED** — needs 12a MatrixKeeperLib first |
| MatrixLogicLib | 21,504 | 3,072 | 1, 11 | room, but it absorbs TierRouter overflow too — watch it |
| FigureEightMatrixV8 | 14,212 | 10,364 | 1 | clear |
| PairManagerV8 | 13,528 | 11,048 | 7, 10 | clear |
| StabilityFund | 13,372 | 11,204 | — | clear |
| V8Governance | 12,179 | 12,397 | — | clear |
| CNOVAToken | 12,878 | 11,698 | 4, 8, 9 | clear |
| CNOVADirectSale | 8,832 | 15,744 | 6 | clear |
| CommunityWallet | 7,827 | 16,749 | — | clear |
| CNOVATreasury | 7,138 | 17,438 | 5 | clear |
| CNOVABuybackReserve | 6,392 | 18,184 | — | clear |
| CouponRegistry | 4,271 | 20,305 | — | clear |
| OnrampRewardPool | 3,745 | 20,831 | — | clear |
| TierRouterLib | 3,085 | **21,491** | 2, 3 | the destination — 21 KB of room |

All 16 watched contracts fit today. But three are within 600 bytes of the
limit, and two of the three carry scope items (TierRouter: 2, 3 — MatrixKeeper:
12). That is the single largest schedule risk in V8.48 — bigger than any
individual defect on the list.

The good news the baseline buys us: **every other contract in the release is
under 13 KB.** Items 4/5/6/7/8/9/10/11 all land in contracts with 10 KB+ of
headroom and carry effectively zero size risk. The size problem is not a
release-wide problem — it is exactly two contracts, and both have a library
already built or planned to absorb the overflow (TierRouterLib at 3,085 bytes
has 21 KB free; MatrixKeeperLib per item 12a does not exist yet).

`scripts/sizes.js` WATCH list was itself incomplete: it omitted CNOVATreasury
and CNOVADirectSale, both of which V8.48 modifies (items 5 and 6). Extended
8 -> 16 contracts, and the gate now covers every contract the release touches.

**Rule for this release:** run `scripts/sizes.js` after EVERY contract edit, not
at the end. On a 142-byte budget, discovering the overflow at deploy time costs
a full re-architecture round.

## THE HARD CONSTRAINT — read before writing any code

**`TierRouter` had +142 bytes of EIP-170 headroom at V8.47.** Items 2 and 3 touch
it. They MUST go into `TierRouterLib`, not the router. This is physics, not
preference — the deploy simply fails otherwise.

Run `npx hardhat run scripts/sizes.js` BEFORE and AFTER every contract edit.
If any contract crosses 24,576 bytes the release cannot ship, and the only
remedies are library extraction or splitting the release. Establish the
before-baseline first.

## Suggested order (dependency, not priority)

1. **Sizes baseline** — `scripts/sizes.js`, record every contract.
2. **#10b `_findExternalPair` + #10 `rescueReentry` — SHIP TOGETHER.** 10 alone is
   unsafe: it redirects rescued members from MatB to MatA, and rescues are MatB's
   dominant feed today (T1 MatA rot 761 vs MatB 3,216). 10b is what makes 10 safe —
   it restores new-registration flow to every pair's MatA, so MatA rotation climbs,
   so crossings climb, so MatB is fed through its DESIGNED channel
   (`_finalizeCrossing` MatA -> partner) instead of through a bug's side effect.
   Fixing either one alone starves the other half.
3. **#11 surplus** — money loss, self-contained, same file.
4. **#4/#5/#6 floor set** — one-liners, already decided, already live in copy.
5. **#8/#9 vest ledger** — self-contained in CNOVAToken.
6. **#1/#2/#3 view parity + bulkWithdraw** — biggest size risk, do while headroom is known.
7. **#7/#13/#14 exit penalty + wiring + gate** — needs the design decision on cross-pair join dates.
8. **#12a MatrixKeeperLib extraction**, then **#12 checkUpkeep discovery** — largest new surface; retires 3 keepers. Extraction first or 12 cannot compile.

## Test gates before deploy

- Full suite green (402+ tests at V8.45; expect additions per item)
- New regression per numbered item — every one above is a live-verified defect,
  so each has a reproducible case
- `predeploy_check.js` passes INCLUDING the new wiring assertions (#14)
- Contract sizes under 24,576 bytes
- Keepers-OFF rotation gate (#20) on the fresh deploy
