# CNFS Daily Analysis — 2026-05-28

**Verdict: Clean sweep — 100% win rate on 6 CNFS trades, all trail-closed after BE, $4.07 net; 1 fresh signal fired at 14:26 UTC. No stale holds, no losses. TP hit rate 0% is expected given current trail-close-at-BE config.**

---

## 1. Today's Trade Summary

The trades log contains **12 rows** for 2026-05-28, but these break into two distinct groups:

| Group | Tickets | Count | Description |
|---|---|---|---|
| CNFS Signal Trades | 93xxxxxx | 6 | Managed with SL/TP, `be_done=True` |
| SYNC / Legacy Closes | 90xxxxxx | 6 | No SL/TP set, negative durations — positions opened prior day, closed today via SYNC |

**CNFS Signal Trades (primary analysis):**

| Metric | Value |
|---|---|
| Total trades | 6 |
| Wins (pnl > $0.50) | 6 (100%) |
| Losses (pnl < -$0.50) | 0 |
| Break-evens | 0 |
| Net P&L | **+$4.07** |
| Win rate (excl BE) | **100%** |
| TP hits (`tp_hit == True`) | **0 (0%)** |
| Avg duration | 16.8 min |
| Avg P&L per trade | $0.68 |

**Context on 0% TP hit rate:** All 6 wins were trail-closed after BE (`be_done=True`). This is the expected behaviour under the current config — the trailing stop locks in profit before price reaches the hard TP level. This is not a failure mode; TP=False simply means the exit happened via trail, not the limit order.

**SYNC / Legacy Trades (secondary):**
- Identical P&L values to the CNFS trades (paired positions from the 247 bot on the same signals).
- Negative `duration_min` values (e.g. -155.9 min) are a **data artifact** — `time_close` precedes `time_open` because these positions opened on 2026-05-27 but the `date` column reflects the close date (2026-05-28). No action required, but worth noting for any future log analysis tooling.
- Combined P&L including SYNC closes: **+$8.14** (matches `trades_log.csv` total).
- The EOD event log reports `8 trades, P&L=+$7.14` — minor discrepancy likely due to different counting scope (BOT's internal vs CSV log).

---

## 2. Time-of-Day Breakdown (CNFS Trades, UTC)

| Hour (UTC) | Trades | W/L/BE | Avg P&L | TP Rate | Note |
|---|---|---|---|---|---|
| 04 | 1 | 1/0/0 | $0.52 | 0% | |
| 05 | 1 | 1/0/0 | $0.65 | 0% | |
| 07 | 2 | 2/0/0 | $0.65 | 0% | |
| 09 | 1 | 1/0/0 | $0.68 | 0% | |
| 10 | 1 | 1/0/0 | $0.93 | 0% | Best hour |

All trades fell within the 04–19 UTC window. **No outside-hours activity.** No dead zones (every hour was profitable).

*Note: `hour_open` is stored in UTC; `time_open` in the CSV appears to be in EDT (UTC−4), which is consistent.*

---

## 3. Duration Analysis (CNFS Trades)

| Exit Type | Count | Avg Duration |
|---|---|---|
| TP hits | 0 | N/A |
| SL hits | 0 | N/A |
| Trail/BE closes | 6 | 16.8 min |

Individual durations: 24.1 min, 2.8 min, 33.3 min, 25.4 min, 9.7 min, 5.3 min.

**Stale holds (>120 min): 0.**

No trades ran long. The tightest exit was 2.8 min and longest 33.3 min — all within a half-hour. The widened trail ($5.00 / MIN_TRAIL_PIPS=50) appears to be letting trades breathe without holding too long.

---

## 4. Signal-to-Execution Comparison

| Metric | Value |
|---|---|
| Signals in `free_signals_log.csv` | 0 |
| Signals in `events.log` | 1 (BUY @ 14:26 UTC) |
| Trades executed today | 6 CNFS (incl. 1 from today's signal) |

**Gap:** `free_signals_log.csv` is empty (header only) despite a confirmed signal broadcast at 14:26 UTC for `XAUUSD BUY E=4462.97 SL=4433.50 TP=4507.18`. This suggests the signal logger is not writing to the CSV — likely a code path issue where the broadcast happens but the CSV append is skipped (possibly a file handle or conditional bug). The signal itself DID execute (ticket 93187472, P&L=$0.93). **Recommend investigating the `free_signals_log.csv` write path.**

The bot started scanning at **13:34 UTC** today. The 5 earlier CNFS trades (before 13:34 UTC) are carry-overs from signals generated on 2026-05-27 that were already open when today started.

---

## 5. Key Findings vs Baseline (2026-05-11)

| Metric | Baseline Expectation | Today's Actual | Status |
|---|---|---|---|
| Outside-hours trades | 30% of signals | 0% | ✅ Improved (or no overnight signals fired) |
| TP hit rate | >40% (after `REMOVE_TP_ON_BE=False` fix) | 0% | ⚠️ See note below |
| Avg duration (winners) | Shorter than pre-fix | 16.8 min | ✅ No stale holds |
| Trail width ($5.00) | Should survive M5 noise | Confirmed — no premature stops | ✅ |
| Avg P&L per trade | ~$13–15 (from backtest) | $0.68 | ⚠️ See note below |

**On TP hit rate and avg P&L:** The $0.68 avg and 0% TP rate indicate trades are being trailed out well short of the TP targets (e.g. today's BUY had TP=$4507.18 but closed at $4464.07, capturing $0.93 on a $44 target). This is the trail-after-BE mechanism doing its job — it secures profit early but foregoes the full TP. If the intent is to capture larger moves, consider whether the trail trigger (BE buffer) is being set too tightly or whether the trail distance should widen further to let winners run. The backtest avg of $13–15 likely assumed full TP captures; the current config prioritises safety over size.

---

## 6. Recommendation Flags

| Flag | Triggered | Detail |
|---|---|---|
| TP hit rate < 40% | ✅ Yes (0%) | Not a loss issue — all trades won. But trail is exiting far from TP. Consider if this is intentional or if trail should widen to capture more of the move. |
| Avg non-TP duration > 60 min | ❌ No (16.8 min) | Trade management is responsive. |
| Outside-hours losses | ❌ No | No outside-hours trades today. |
| Dead zone (0% win, 3+ trades) | ❌ No | Every active hour was profitable. |
| `free_signals_log.csv` not writing | ⚠️ **NEW FLAG** | Signal fired and executed but CSV shows 0 entries. Investigate write path in signal broadcast code. |

---

## Bot Status

Bot is **running and healthy**. Events log confirms continuous 30-second scans as of 02:01 UTC on 2026-05-29, with recent skip reasons being "no doji" and "no clean pullback" — normal filter behaviour in a trending/non-setup market.

---

## What to Watch Tomorrow

1. **`free_signals_log.csv` write bug** — if it stays empty tomorrow despite signals firing, trace the broadcast code path. The log gap makes signal-vs-execution reconciliation unreliable.
2. **Trail vs TP gap** — today's BUY captured $0.93 of a potential $44 move. If this pattern repeats, evaluate whether raising the trail lock-in distance could capture meaningfully more without sacrificing win rate.
3. **Carry-over positions** — any signals from today (14:26 UTC BUY) that didn't fully close may appear as early closes in tomorrow's log. The negative-duration SYNC artifact will reappear; not a bug, just the logging pattern.
