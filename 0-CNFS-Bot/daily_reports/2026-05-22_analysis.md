# CNFS Daily Analysis — 2026-05-22

**Verdict: No trades closed today — bot is running and scanning, but a signal at 10:07 UTC fired without producing an executed trade. Possible execution/logging gap. Investigate MT5 order submission.**

---

## 1. Today's Trade Summary

| Metric | Value |
|--------|-------|
| Trades closed today | **0** |
| Wins / Losses / BEs | — |
| Net P&L | **$0.00** |
| Win rate (excl BE) | — |
| TP hit count / rate | — |
| Avg duration (TP hits) | — |
| Avg duration (non-TP) | — |

`trades_log.csv` contains **no entries dated 2026-05-22**. The most recent closed trade is from 2026-05-21 22:07 UTC.

**However, the bot IS running.** `events.log` shows continuous 30-second scans through at least 14:32 UTC today. The bot restarted three times:

| Restart | UTC Time | Balance | Last trade age |
|---------|----------|---------|---------------|
| 1 | 07:17 | $1,201.28 | 18,736 s (~5.2 h) cooldown clear |
| 2 | 15:50 | $1,201.84 | 20,615 s (~5.7 h) cooldown clear |
| 3 | 16:41 | $1,201.84 | 23,669 s (~6.6 h) cooldown clear |

Balance increased by **$0.56** between restart 1 and restart 2, suggesting a trade may have closed on MT5 that was not logged locally. All restarts found **0 open positions to adopt**.

---

## 2. Time-of-Day Breakdown

No trades were closed today, so no hourly breakdown is possible for 2026-05-22.

For reference, the hourly picture across recent history (2026-05-20–21, 8 trades) is shown in Section 5.

---

## 3. Duration Analysis

No closed trades today. No stale holds (no open positions confirmed at 15:50 and 16:41 UTC restarts).

**Historical note (2026-05-20–21):** Average duration across all 8 trades was **10.88 min** (range 0–23.8 min). No stale holds (>120 min) have occurred. Trades are exiting very quickly, which is consistent with tight BE trail execution rather than TP hits.

---

## 4. Signal-to-Execution Comparison

| Metric | Value |
|--------|-------|
| Signals generated today (`free_signals_log.csv`) | **0 logged** |
| Signal events in `events.log` | **1** (BUY at 10:07 UTC) |
| Trades executed today | **0** |
| Execution gap | **1 unexecuted signal** |

### Signal Detail
```
2026-05-22T10:07:02 UTC
*** SIGNAL BUY | Entry=4533.25  SL=4524.86  TP=4545.84  ATR=42.0p ***
```

The signal fired within the valid session window (10:07 UTC is inside 04–19 UTC). After the signal line, `events.log` shows only `skip: same candle already acted on` then normal scans — **no order confirmation, no trade ticket, no error message.**

`last_trade_time.txt` records `1779444422.703038` = **2026-05-22 10:07:02 UTC**, confirming the bot updated its cooldown timer at signal time, but no trade appears in `trades_log.csv`.

**`free_signals_log.csv` is empty (header only) and `free_signals.log` is 0 bytes.** Signal logging is not functioning — this is a pre-existing issue that should be investigated alongside the execution gap.

### Possible Causes for Unexecuted Signal
1. **Silent MT5 order rejection** — The bot set `last_trade_time` but the `order_send()` call to MT5 returned an error that was not logged. Check `main.py` for whether MT5 error codes are caught and logged.
2. **Max position cap** — Unlikely; restarts confirmed 0 open positions.
3. **Lot size / margin check** — If lot calculation returns 0 or fails validation, the trade may be skipped silently.
4. **Balance discrepancy** — The $0.56 balance increase between restarts (without a logged trade) may point to a trade that executed and closed on the broker side but was never written to `trades_log.csv`.

---

## 5. Key Findings vs Baseline (2026-05-11 Analysis)

Using the 8 available trades from 2026-05-20–21 as the current reference set:

| Metric | Baseline Expectation | Actual (8 trades) |
|--------|---------------------|-------------------|
| TP hit rate | Improvement from `REMOVE_TP_ON_BE=False` | **0%** (0/8 TP hits) |
| Avg trade duration | Longer holds reaching TP | **10.88 min avg** — very fast exits |
| Avg P&L per win | ~$13–15 (TP level) | **$2.39** (trail/BE exits) |
| Signal outside 04–19 UTC | 30% of signals | Outside-hours trades: 3/8 = 37.5% |
| Trail survival | Widened to $5 (MIN_TRAIL_PIPS=50) | Avg win <$3 suggests trail still triggering early OR price moving fast |
| Net P&L | Positive with TP hit improvement | **+$3.92** (8 trades, 1 large loss of -$10.90) |

**TP hit rate remains 0%.** This is the most significant finding. Either:
- `REMOVE_TP_ON_BE=False` is not yet in effect in the deployed config, or
- The trail is still tightening and triggering before price reaches TP, or
- Trades are closing profitably via BE trail but well short of the original TP target.

All wins have `be_done=True` and `tp_hit=False`, confirming trades are closing via the BE/trailing mechanism, not TP orders.

### Hourly performance across all available history (2026-05-20–21):

| UTC Hour | Trades | W/L/BE | Avg P&L | In Session? |
|----------|--------|---------|---------|-------------|
| 00h | 1 | 1/0/0 | +$1.46 | No |
| 01h | 1 | 1/0/0 | +$3.86 | No |
| 02h | 1 | 1/0/0 | +$1.13 | No |
| 06h | 1 | 1/0/0 | +$3.04 | Yes |
| 07h | 1 | 1/0/0 | +$0.95 | Yes |
| 09h | 1 | 0/0/1 | +$0.46 | Yes |
| 10h | 1 | 0/1/0 | **-$10.90** | Yes |
| 18h | 1 | 1/0/0 | +$3.92 | Yes |

Outside-hours trades (UTC 00–04): 3 trades, 3 wins, net **+$6.45**, avg **+$2.15**. Outside-hours trades are performing *better* in this small sample — the baseline concern about Asian-session dead weight has not materialised so far, though N=3 is insufficient to draw conclusions.

The only loss (-$10.90 at UTC 10h on 2026-05-21) dominates the P&L picture and is nearly 4.6× the average win. This is the same trade flagged in the 2026-05-21 report.

---

## 6. Recommendation Flags

| Flag | Status | Detail |
|------|--------|--------|
| TP hit rate < 40% | ⚠️ **TRIGGERED** | 0% TP hit rate across 8 trades. Trail or BE mechanism is closing all trades before TP. Verify `REMOVE_TP_ON_BE=False` is live in `config.py`. |
| Avg non-TP duration > 60 min | ✅ Clear | Avg 10.88 min — exits are fast. No SL drag issue. |
| Outside-hours losses dominate | ✅ Clear | Outside hours trades are profitable in current sample (3/3 wins). Time filter not yet warranted but monitor as sample grows. |
| Dead zone (0% win rate, 3+ trades) | ✅ Clear | No hour has 3+ trades yet. UTC 10h has 1 loss — watch if a pattern forms. |
| **NEW: Signal execution gap** | 🚨 **ACTION REQUIRED** | Signal fired at 10:07 UTC with no resulting trade. `free_signals_log.csv` and `free_signals.log` are both empty — signal logging is broken. Check `main.py` for MT5 order error handling and signal log write path. |
| **NEW: Balance discrepancy** | ⚠️ **Investigate** | Balance rose $0.56 without a logged trade. Possible trade executed on broker side but not captured locally. |

---

## What to Watch Tomorrow

1. **Top priority — execution gap:** Check `main.py` to confirm MT5 order errors are being caught and logged. The signal at 10:07 set the cooldown timer but left no trade record — this could mean valid setups are being silently skipped.
2. **Signal logging:** `free_signals_log.csv` and `free_signals.log` are both empty. Confirm the signal-write code paths in `main.py` are still wired up correctly after recent edits.
3. **TP hit rate:** Still 0% across all logged trades. If another 3–5 trades close tomorrow without hitting TP, the trail configuration (`REMOVE_TP_ON_BE`, `MIN_TRAIL_PIPS=50`) needs a re-check against the live `config.py`.
4. **UTC 10h zone:** Today's signal (10:07 UTC) was in the same hour as yesterday's -$10.90 loss. If another trade fires here, watch whether the wider trail survives or if the same pattern repeats.
5. **Bot stability:** Three restarts today is elevated. Confirm the process manager is stable and restarts are intentional (e.g. scheduled), not crash-recovery.

---

*Report generated automatically by CNFS daily analysis task — 2026-05-22*
