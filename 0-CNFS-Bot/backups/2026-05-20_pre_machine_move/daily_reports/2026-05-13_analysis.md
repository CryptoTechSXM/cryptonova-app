# CNFS Daily Analysis — 2026-05-13

**Headline verdict:** *Quiet, inconclusive day* — only 1 trade closed (a +$7.91 winner that rolled over from the May‑12 late session) and only 1 fresh signal generated, which never produced a trade row. **Bot stopped by user at 17:14 UTC and did not restart** — no scanning in the 17:15‑23:59 UTC window. Insufficient sample to evaluate the new trail / `REMOVE_TP_ON_BE` parameters today.

---

## 1. Today's trade summary

Filtering `trades_log.csv` for `date == 2026-05-13` returns a single row:

| date | time_open | time_close | ticket | dir | entry | close | pnl | tp_hit | be_done | hour_open |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-05-13 | 02:03:05 | 00:05:39 | 92691928 | BUY | 4718.09 | 4726.00 | **+7.91** | False | True | 2 |

| Metric | Value |
|---|---|
| Total trades closed | **1** |
| Wins (pnl > $0.50) | 1 (+$7.91) |
| Losses (pnl < ‑$0.50) | 0 |
| BEs (\|pnl\| ≤ $0.50) | 0 |
| Net P&L | **+$7.91** |
| Win rate (excl BE) | 100% (1/1) |
| TP hit rate | **0 / 1 = 0.0%** |
| Avg duration — TP hits | n/a |
| Avg duration — non‑TP closes | ‑117.4 min *(data anomaly — see notes)* |

Notes:
- This is the "third winning trade" carried forward from yesterday's report — it opened late on May 12 (02:03 UTC), bridged midnight, and closed at 00:05 on May 13. The CSV's `duration_min` field is being computed as `time_close − time_open` on the same calendar day, hence the negative value. The same bug produced negative durations on three rows yesterday (tickets 92689690, 92689941). **Filed as a CSV-writer bug**, not a trading issue.
- The winner closed via **trail**, not TP — consistent with yesterday's pattern of trail‑driven exits.

## 2. Time‑of‑day breakdown (UTC)

| Hour | N | W | L | BE | Avg P&L | TP rate |
|---:|---:|---:|---:|---:|---:|---:|
| 02 | 1 | 1 | 0 | 0 | +$7.91 | 0% |

**Outside 04–19 UTC:** the only closed trade today (hour 02 UTC) is outside the canonical session — but it's a holdover, not a freshly opened May‑13 trade. Sample size too small to flag as a dead zone or recommend a time filter from today alone.

No hour with losses dominating. No hour with 3+ trades and 0% win rate.

## 3. Duration analysis

| Class | n | Avg duration (min) |
|---|---:|---:|
| TP hits | 0 | — |
| SL hits | 0 | — |
| BE closes | 0 | — |
| Trail closes (today's winner) | 1 | n/a (negative due to midnight wrap) |

**Stale holds (>120 min open):** None observed in today's CSV rows. The lone closer's true wall‑clock duration was ~122 min (02:03 May 13 → 00:05 May 14‑equiv crossing midnight), borderline but not flagged.

## 4. Signal‑to‑execution comparison

| Source | Count |
|---|---|
| `free_signals_log.csv` rows for today | **0** (file contains only header) |
| `events.log` SIGNAL lines on 2026‑05‑13 | **1** — *BUY @ 4701.22, SL 4692.53, TP 4714.25, ATR 43.4p, 16:22 UTC* |
| Trades opened today in `trades_log.csv` | **0** matching that signal |
| Scan events on 2026‑05‑13 | 884 (13:33 UTC → 17:14 UTC only) |

**Discrepancies:**
1. `free_signals_log.csv` was not appended to despite a confirmed signal in `events.log`. Possible regression in the signal‑CSV writer after the 2026‑05‑10 fix; worth checking the writer code path.
2. The 16:22 BUY signal has no corresponding row in `trades_log.csv`. The next scan one tick later logged "skip: same candle already acted on" — meaning the bot did *act* on it (so it attempted to execute). Either the open is still live (would only flush to `trades_log.csv` on close) or it was rejected by the broker / position cap. **Cannot determine without restarting the bot and querying open positions.**
3. **The bot logged "Bot stopped by user." at 17:14:54 UTC and did not resume.** Roughly 7 hours of the regular session (17–24 UTC) plus the entire early Asian window of the next day were missed.
4. `heartbeat.txt` is **3 days stale** (timestamp resolves to 2026‑05‑11 00:10 UTC). Either the heartbeat writer hasn't been wired up since the last refactor, or it's only updated on a path the bot didn't reach today.

## 5. Key findings vs baseline (2026‑05‑11)

| Baseline expectation | Today's actual |
|---|---|
| 30% of signals outside 04–19 UTC | 0/1 signals outside session (16:22 UTC is well inside) — but sample = 1 |
| Avg Free TP target: $15.20 | n/a (no TP hits) |
| `REMOVE_TP_ON_BE=False` should close at TP | Not testable — the only closer used trail; the only fresh signal didn't close yet |
| Trail $5.00 / MIN_TRAIL_PIPS=50 should survive M5 noise | Did not stop out the live trade prematurely — the carried winner closed clean at +$7.91 via trail |

**Net:** the trail change is *not contradicting* expectations (the carried trade closed positive and was not stopped out by noise), but with one data point and zero TP fills, **the central baseline question — does TP hit rate now exceed 40% — remains unanswered.**

## 6. Recommendation flags

| Trigger | Tripped? | Action |
|---|---|---|
| TP hit rate < 40% | ✗ N/A (n=1) | Insufficient data — re‑evaluate after 5+ closes |
| Avg duration for non‑TP > 60 min | ✗ N/A | — |
| Outside‑hours trades have worse outcomes | ✗ N/A | — |
| Any hour with 0% wins & 3+ trades | ✗ | — |
| **Bot offline mid‑session** | ✅ **YES** | **Restart investigation:** bot stopped at 17:14 UTC by user; no autostart. Confirm whether this was intentional and re‑enable before tomorrow's London open. |
| **Signal CSV writer not firing** | ✅ **YES** | `free_signals_log.csv` has the header only despite 1 confirmed signal in `events.log`. Check the writer hook in `main.py` / `0a-Free-Signals-main.py` — last edited 2026‑05‑13 00:35 / 2026‑05‑06. |
| **Heartbeat 3 days stale** | ✅ **YES** | Either wire the heartbeat into the main scan loop or stop relying on it for liveness checks. |
| **`duration_min` negative on midnight crossings** | ✅ **YES** | Trivial fix in the trade‑close logger: compute duration off the timezone‑aware datetime, not `HH:MM` strings. |

## What to watch tomorrow

1. **Bot uptime first.** Confirm CNFS is running before the 04 UTC start; check `events.log` shows continuous scanning past 19 UTC.
2. **Resolve the 16:22 BUY trade.** If it's still open (entry 4701.22, SL 4692.53, TP 4714.25), tomorrow's report should capture its close — that's the first real test of the new `REMOVE_TP_ON_BE=False` + widened trail combo on a freshly‑opened May‑13 signal.
3. **Verify signal CSV writes** the moment a new signal is generated. If the next signal also fails to append, escalate the writer bug above any strategy tuning.
4. **Need volume.** Two consecutive thin days (18 closes on May 12, 1 on May 13) mean we still can't validate the trail/TP changes. If signal frequency stays this low, the issue is upstream of TP hit rate — strategy filters (no doji / no clean pullback / EMA‑H1 mismatch) accounted for ~99.9% of today's 884 scans.

---

*Generated automatically by `cnfs-daily-analysis` scheduled task.*
