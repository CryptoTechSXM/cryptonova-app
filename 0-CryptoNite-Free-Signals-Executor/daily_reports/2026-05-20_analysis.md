# CNFS Daily Analysis — 2026-05-20

**Headline:** Quiet, clean day — 2/2 closed trades won (+$5.38 net), but **0% TP hit**: the new $2.00 trail (lowered today) is banking small wins early instead of reaching TP. No stale holds. ⚠️ Signal CSV logger is empty — needs a fix.

> Note on timezones: `time_open`/`time_close` in the trades log are local (EDT); `hour_open` is the UTC hour. `events.log` timestamps are UTC. "Today" = the EDT trading day 2026-05-20, which spans roughly 14:00 UTC 05-20 → 02:00 UTC 05-21.

---

## 1. Today's trade summary

| Metric | Value |
|---|---|
| Total trades closed today | **2** |
| Wins (pnl > 0.50) | 2 |
| Losses (pnl < -0.50) | 0 |
| Break-even (\|pnl\| ≤ 0.50) | 0 |
| **Net P&L** | **+$5.38** |
| Win rate (excl. BE) | **100%** (2/2) |
| TP hit count | **0** |
| TP hit rate | **0%** |
| Avg duration — TP hits | n/a (no TP hits) |
| Avg duration — non-TP hits | 10.7 min (incl. adopted trade) / **21.4 min** (excl. the duration-0 adopted trade) |

Both trades closed in profit via the **trailing stop after break-even lock** (`be_done=True`, `tp_hit=False`) — neither ran all the way to its TP target.

| Ticket | Dir | Entry | Close | PnL | Pips | Dur (min) | TP hit | hour_open (UTC) |
|---|---|---|---|---|---|---|---|---|
| 92967964 | BUY | 4537.20 | 4541.12 | +3.92 | 39.2 | 0.0* | False | 18 |
| 92978510 | BUY | 4544.51 | 4546.17 | +1.46 | 16.6 | 21.4 | False | 00 |

\*Ticket 92967964 was an **adopted position** — the bot restarted at 18:09 UTC and reported *"Found 1 open position(s) to adopt"*. Open-time tracking was lost on adoption, so `duration_min` logged as 0.0 (not a real instant close).

---

## 2. Time-of-day breakdown (by `hour_open`, UTC)

| Hour (UTC) | Trades | W / L / BE | Avg P&L | TP hit rate | In 04–19 session? |
|---|---|---|---|---|---|
| 18 | 1 | 1 / 0 / 0 | +$3.92 | 0% | ✅ yes |
| 00 | 1 | 1 / 0 / 0 | +$1.46 | 0% | ❌ no (outside-hours) |

The hour-00 UTC trade falls outside the 04–19 window (Asian/overnight session flagged in the baseline). Today it **won**, so losses did not dominate — but the sample is a single trade, far too small to draw a conclusion. No outside-hours window showed loss dominance today.

---

## 3. Duration analysis

- **TP hits:** none today.
- **SL hits:** none today.
- **BE closes (|pnl| ≤ 0.50):** none — both trades locked BE then trailed out in profit.
- **Trade durations:** ticket …964 = 0.0 min (adoption artifact, see §1); ticket …510 = **21.4 min**.
- **Stale holds (> 120 min):** **none.** Longest genuine hold was 21.4 min.

Takeaway: trades are closing **fast** (~21 min), not drifting. The risk today is the opposite of stale holds — the tightened trail is closing positions early, before TP.

---

## 4. Signal-to-execution comparison

- **`free_signals_log.csv`: 0 signals logged** (header only). The CSV signal logger is **not writing** — the file (along with `free_signals.log` and `heartbeat.txt`) was reset/truncated around the 18:05–18:09 UTC restart and has stayed empty since. ⚠️ **Flag for fix** — without it, the signal-vs-execution comparison can't be done from the intended source.
- **From `events.log` instead** (the live scan log, which is healthy and current to 02:01 UTC 05-21), 2 explicit signals fired in the visible post-restart window:
  - `00:35 UTC — SIGNAL BUY Entry=4544.19 SL=4533.32 TP=4560.49` → executed as ticket 92978510 (closed +1.46).
  - `01:38 UTC — SIGNAL BUY Entry=4563.77 SL=4551.30 TP=4582.47` → executed (`last_trade_time` = 01:38:12 UTC); **still open** at last scan, not yet in trades_log.
  - Ticket 92967964 was opened **before** the restart (adopted at 18:09 UTC); its signal line is in the now-rotated pre-restart log.
- **Net:** ~3 trades initiated today (2 closed + 1 open) against ~3 signals → roughly **1:1**. No signal glut, **no position-cap blocking** observed. The only post-signal skips were *"same candle already acted on"* (normal dedupe).
- **Why so few trades?** The dominant gate today was the **ATR floor**. The skip log is overwhelmingly *"ATR too low — floor (X < 4.0)"* (hundreds of occurrences, e.g. 130× at ATR 2.52), plus *"no doji"* (148×) and *"EMA says SELL but H1 says BUY"* (35×). Volatility sat below the `MIN_ATR=4.0` floor for most of the session — by design (floor raised 2026-05-19), so very few setups qualified.

---

## 5. Key findings vs baseline (2026-05-11)

| Baseline expectation | Today's actual | Verdict |
|---|---|---|
| ~30% of signals outside 04–19 UTC tie up slots | 1 of 2 closed (50%) opened at hour-00 UTC; it won, didn't tie up a slot | Small sample; outside-hours not harmful today |
| Avg Free signal TP ≈ $15.20 | TP targets were ~$26 (…964) and ~$16 (…510); **neither reached** | TP not the exit driver today |
| `REMOVE_TP_ON_BE=False` → trades close **AT TP** rather than drifting | TP hit rate **0%** — both closed via trail at +$3.92 / +$1.46, well short of TP | ❌ Not happening — trail fires first |
| Trail widened to $5.00 (`MIN_TRAIL_PIPS=50`) to survive M5 noise | **Config changed today:** `MIN_TRAIL_PIPS` lowered 30→**20 ($2.00)** and BE trigger 40→**20 ($2.00)** on 2026-05-20 | Baseline note is **stale** — trail is now much **tighter** than $5.00 |

The baseline's $5.00 trail assumption no longer holds — the trail and BE trigger were both retuned tighter to $2.00 today. That directly explains why both winners closed early via the trail (small gains) instead of running to TP. Today's outcomes reflect the **new, tighter** configuration, not the baseline one.

---

## 6. Recommendation flags

- 🚩 **TP hit rate < 40% (actual 0%).** Triggered. The just-lowered $2.00 trail is closing trades before TP. **Watch whether the 30→20 trail change is too aggressive** — it converts would-be TP runs into ~$2–4 scalps. *Caveat: only 2 closed trades — a tiny sample; don't over-correct on one day.*
- ✅ **Avg non-TP duration > 60 min?** No — only 21.4 min. The SL/trail is moving plenty fast; if anything, **too** fast (opposite of the "SL not moving" concern).
- ⚪ **Outside-hours worse?** Inconclusive. The one hour-00 UTC trade won (+$1.46, the smaller winner). Not enough data to recommend a time filter yet.
- ✅ **Any hour with 0% win rate and 3+ trades (dead zone)?** No — no hour had 3+ trades.
- 🚩 **Signal CSV logger empty.** `free_signals_log.csv` has not recorded a single signal since the ~18:05 UTC reset. Restore signal-CSV logging so future signal-vs-execution analysis is reliable.
- ℹ️ **ATR floor heavily gated activity.** Market volatility was below `MIN_ATR=4.0` for most of the day. Expected after the 05-19 floor raise, but worth monitoring across several quiet days that it isn't over-filtering genuine setups.

---

## What to watch tomorrow

1. **Fix / confirm the signal CSV logger** — without it, the daily signal-vs-trade comparison falls back to scraping `events.log`.
2. **TP vs trail balance.** With the new $2.00 trail + $2.00 BE trigger, watch whether TP hit rate stays near 0%. If a larger sample keeps closing trades at ~$2–4 well short of TP, the trail is too tight and is capping upside — consider widening it back toward the $3–5 range.
3. **Track the open trade** (ticket from the 01:38 UTC signal, entry ~4563.77, TP 4582.47) — see whether it reaches TP or trails out, as another data point on the tight-trail question.
4. **ATR floor impact.** If quiet conditions persist and trade count stays at 1–3/day, reassess whether `MIN_ATR=4.0` is the right floor for the current regime.

*Bot status: healthy. Scanning every ~30s, last scan 02:01 UTC 2026-05-21, balance ~$1,198.16 (TMFinancials, XAUUSD.f).*
