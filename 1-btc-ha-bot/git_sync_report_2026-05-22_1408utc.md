# Git Sync Report — 2026-05-22 14:08 UTC

## Status: ❌ UNABLE TO RUN — git_sync.py not accessible

## What happened

The scheduled task attempted to execute:
```
python C:\CryptoNite-MT5-Bots\git_sync.py
```

The script **was not found**. Only the following subdirectories are mounted in the Cowork sandbox:
- `C:\CryptoNite-MT5-Bots\1-btc-ha-bot`
- `C:\CryptoNite-MT5-Bots\6-Quick-Scalp-NAS100`
- `C:\CryptoNite-MT5-Bots\CryptoNite-Free-Signals`

The **parent folder** `C:\CryptoNite-MT5-Bots\` (where `git_sync.py` lives) is **not mounted**, so the sandbox cannot see or execute the script.

## Why this keeps failing

This same issue has been present in every run since 2026-05-10 (now 12+ days). The Cowork sandbox only sees the three subfolder mounts, not the parent `C:\CryptoNite-MT5-Bots\` directory where `git_sync.py` resides. The monorepo's `.git/` directory is also in that unmounted parent, so even replicating the sync logic manually is not possible.

## Recommended Fix (pick one)

**Option A — Windows Task Scheduler (recommended)**
Create a Task Scheduler job that runs directly:
```
python C:\CryptoNite-MT5-Bots\git_sync.py
```
This runs natively on Windows with full access to all files and git credentials. Delete this Cowork scheduled task once set up.

**Option B — Mount the parent folder in Cowork**
In Cowork, remove the three individual subfolder mounts and instead add `C:\CryptoNite-MT5-Bots` as the selected folder. This gives the sandbox access to `git_sync.py` and the `.git/` directory. You would also need git installed and credentials configured in the sandbox.

---
*This report was auto-generated by the cryptonite-git-sync scheduled task.*
